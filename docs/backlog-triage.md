# Backlog triage

**Generated by `scripts/backlog-triage.php`. Do not hand-edit — regenerate it.**

**Measured 2026-09-05** against `origin/main` at `593a900af3a07e9e67396d1ccaf7db870741d3a6` · 91 open rows · 0 `major` · 91 `minor` · 0 `nit`

⚠️ **This is an OPERABILITY order, not a priority order, and the difference is why the file it
replaced went wrong.** It cannot tell you what matters most. Severity would, and there are zero
open `major` rows, so that field separates nothing. What it derives is which rows are still
**live**, whose **citations still resolve**, and which rows **share no files** with each other —
which is what picking a batch needs, and is all this file claims.

⛔ **Liveness here is a marker a human wrote, not a measurement this script made.** It is gated
for presence by `tests/Feature/Docs/BacklogProvenanceTest.php` and reported by
`scripts/state.php`; neither checks that a verdict is *right*. A row marked live that is dead
passes everything. The mutation harness is what settles a row, and settling it is the job of
whichever increment takes it.

📌 **The 2026-08-28 census this file replaced is preserved byte-for-byte in `docs/backlog-triage-m37.md`.**
Its findings about the sweep's own accuracy, and the brief it ran under, are not reproduced here
and are not stale — they are a dated record and are still worth reading before any fan-out.

---

## Census

| | Rows |
|---|---|
| Open | **91** |
| — still `live` | **49** |
| — `latent`, needs a stated precondition | 16 |
| — `not live`, not a defect in this tree | 26 |
| Severity bullets, ever | 217 |

## The queue — live rows, most operable first

Ordered by citation health, then by how few non-hub files the row touches, then oldest
debt first. `cites` lists only the files a batch would collide on; hub files are excluded
from it and listed separately below.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| `tracker-lint` R8 guards `CLAUDE.md` and cannot reach `PROGRESS.md`, which is the half that actually rotted. | `M42` | 4113 | *hub files only* |
| `scripts/mutate.php` cannot drive a positive control for anything that is not Pest in a container. | `M42` | 4228 | *hub files only* |
| Nothing pins `fetch-depth` for the SECRET SCAN, whose blindness is silent and whose stakes are the highest of the three gates that integer governs. | `M49` | 5468 | *hub files only* |
| `submission_geo_index` is a real table and the data dictionary does not mention it at all. | `M58` | 4623 | *hub files only* |
| `scripts/backlog-triage.php` accepts no arguments and its default action is destructive, so any unrecognised flag rewrites the file. | `M70` | 5725 | *hub files only* |
| `docs/data-dictionary.md` documents nine `tenants` columns that exist in no migration, and its enum catalog contradicts the enum. | `M72` | 6070 | *hub files only* |
| `scripts/next.php` never clips the release HEADING, so a third of the hand-off is byte-identical noise. | `M72` | 6102 | *hub files only* |
| The `npm audit` judge makes a REQUIRED context green while nothing was measured, and that is a deliberate trade rather than an oversight. | `M72` | 6119 | *hub files only* |
| The triage generator silently drops any citation written as a PARTIAL path, and a partial path is strictly worse than a bare filename. | `M73` | 6184 | *hub files only* |
| `CLAUDE.md`'s gate table sends PHPStan to the container, one row below the rule that explains why the container is wrong. | `M76` | 6596 | *hub files only* |
| A claim file has no constrained form for a forward declaration, so the one stale declaration on the tree cannot be gated. | `M42` | 4101 | `docs/claims/lane-b.md` |
| `scripts/next.php` takes each release's LEAD paragraph, and a lead paragraph is often a file manifest rather than the lesson. | `M42` | 4248 | `docs/claims/TEMPLATE.md` |
| A tracker surgery whose diff touches only `paths-ignore`d files produces no post-merge run at all, so `R7`'s trunk arm is unreachable for exactly the diff shap… | `M48` | 5346 | `docs/claims/decisions.md` |
| The documented-default gate reads FUNCTION-shaped cells only, so a documented LITERAL that disagrees with the database is invisible to it. | `M58` | 4611 | `tests/Feature/Migrations/DocumentedDefaultDriftTest.php` |
| The audit spec's §1 table is asserted by nothing, and a static sweep cannot be the thing that asserts it. | `M70` | 5705 | `docs/audit-compliance-logging-spec.md` |
| `R7`'s byte threshold was calibrated on surgeries that were all left too late, so acting EARLY lands in its dead zone. | `M71` | 5757 | `scripts/tracker-surgery.php` |
| What actually delivers the offline mis-cased render is unknown, and TWO confident models of it have now been wrong. | `M73` | 6133 | `tests/e2e/public-runtime-offline.spec.ts` |
| A correction still cannot be autosaved, and the reason is an endpoint that does not exist plus a product decision nobody has taken. | `M75` | 6580 | `resources/js/Pages/submissions/Encode.vue` |
| `R7` pins the checkout depth to `PR commits + 1`, so a depth of 50 keeps every gate green while blinding the secret scan to 1,100 of 1,181 commits. | `M76` | 6628 | `scripts/tracker-lint.php` |
| `docker compose run --rm e2e` does not work on this host as three documents prescribe it, and the most likely wrong form EXITS 0. | `M61` | 4844 | `docs/ux/design-system-reference.md` · `docs/ux/exceptions-log.md` |
| The `@throws` contract sweep cannot see the loss of ONE of two declared causes. | `M68` | 1802 | `tests/Feature/Api/OpenApiContractTest.php` · `openapi.json` |
| `docs/backlog-triage.md` is generated stale by its own close-out, and the drift is on the trunk now. | `M71` | 5849 | `docs/feature-backlog.md` · `scripts/loop.php` |
| `guest-shell-assets` has the identical missing status filter, and no row has ever named it. | `M73` | 6149 | `resources/public-runtime/sw.ts` · `vite.config.ts` |
| The proof-of-work yield is pinned on the fallback path only, and its CADENCE is asserted nowhere at all. | `M75` | 6565 | `resources/public-runtime/lib/challenge.ts` · `config/guest.php` |
| Two more scripts have `backlog-triage.php`'s destructive default, and the row that filed that defect names one of them as its harmless counter-example. | `M71` | 5803 | `scripts/regenerate-brand-ramp-fixture.php` · `tests/fixtures/brand-ramp.json` · `scripts/npm-audit-judge.php` · `scripts/tracker-surgery.php` |
| Three gate invocations fetch `http-server` from the network at run time, and nothing declares it. | `M59` | 4742 | *hub files only* |
| `scripts/backlog-triage.php --check` is wired into nothing, so the generated triage can drift with no gate saying so. | `M65` | 3656 | *hub files only* |
| `M59`'s undeclared-`http-server` row undercounts on both of its numbers. | `M71` | 5896 | *hub files only* |
| The partial-path citation row's consequences are mostly false, and the measurement that settles `D15`'s dependency is done. | `M76` | 6646 | *hub files only* |
| PHPUnit's own collector loses 40 test files in the container, and a local run reports green without them. | `M76` | 6612 | `tests/Feature/Docs/SuiteCollectionFloorTest.php` |
| `MdsSegmentedControl` spills 30px INSIDE the builder's config pane — a real horizontal scrollbar, and not the page-overflow defect it was filed as. | `M19` | 630 | `resources/js/components/builder/ConfigPanel.vue` · `packages/design-system/src/components/SegmentedControl/SegmentedControl.vue` · `docs/ux/exceptions-log.md` |
| The service worker caches a credential-bearing resume shell, and only the RENEWAL of it was closed. | `M70` | 5687 | `resources/public-runtime/sw.ts` · `tests/e2e/public-runtime-offline.spec.ts` · `routes/api.php` |
| A media pick made during a conflict review is protected only by the reaper's grace window. | `M22` | 1353 | `resources/public-runtime/App.vue` · `resources/public-runtime/lib/reap.ts` · `resources/public-runtime/lib/replay.ts` · `tsconfig.sw.json` |
| A cluster of by-line citations went stale, several of them inside this branch. | `M1` | 5088 | `app/Support/Tenancy/TenantIsolation.php` · `app/Models/Tenant.php` · `scripts/migration-lint.php` · `config/queue.php` +9 |
| A tabular rule that maps no Submission ID column still duplicates on an unconfirmed retry, and the durable fix is in the rule EDITOR rather than the adapter. | `M5` | 795 | — *no file harvested* |
| `SlackConnector::deliver()` has the same non-idempotent shape and is deliberately not covered. | `M5` | 830 | — *no file harvested* |
| `promote()` re-asserts the version is published BEFORE the lock and never again under it. | `M12` | 1859 | — *no file harvested* |
| The `reviewer` role's seeded description and `SubmissionPolicy::create()` contradict each other. | `M13` | 1693 | — *no file harvested* |
| The tenant-facing SSO domains card on `/settings/sso` does not exist, so verification is operator-assisted. | `M18` | 2323 | — *no file harvested* |
| `MemberController::invite()` validates `['required', 'email', 'max:255']` and a role, with no domain-ownership check. | `M18` | 2338 | — *no file harvested* |
| Self-registration remains a way to occupy an address in a domain you do not control. | `M18` | 2349 | — *no file harvested* |
| The storage-quota line counts strangers' submissions. | `M21` | 1381 | — *no file harvested* |
| Every hand-off prescribes a Pint command that scans ~40 fewer files than CI does. | `M36` | 4005 | — *no file harvested* |
| `PUT /user/profile-information` is a second mail cannon, and it is the one Fortify write route left deliberately unbound. | `M43` | 3209 | — *no file harvested* |
| The promote route still documents neither of its two 403 causes nor its 422, and the generator cannot narrow a refusal family to one route. | `M67` | 1888 | — *no file harvested* |
| The per-item `error.code` is the integrator's real branching key on `/sync/submissions`, and the contract still publishes it as an unconstrained string. | `M69` | 1783 | — *no file harvested* |
| `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index … | `M72` | 6085 | — *no file harvested* |
| What a media cell should SAY beyond a filename, and what shape a grid answer takes in a spreadsheet, are two product questions `M74` deliberately did not answe… | `M74` | 6329 | — *no file harvested* |
| The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s… | `M75` | 6546 | — *no file harvested* |

## Latent — real, but each needs a stated precondition first

Not queue work until the precondition holds. The row says what it is.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| The setup-time directory has no pre-flight refresh | `M1` | 884 | `app/Services/Connectors/TabularDestinationDirectory.php` |
| A verified SSO email domain is trusted indefinitely — there is no re-verification sweep. | `M18` | 2308 | `routes/console.php` |
| `baselineOf()` turns "no checksum" into `''`, and only middleware turns it back. | `M31` | 3004 | `tests/Feature/Submissions/SubmissionEditRoutesTest.php` |
| `gamification:backfill --sync` reports failure after it has already committed every award. | `M32` | 3908 | `app/Console/Commands/BackfillGamificationCommand.php` |
| Route-model binding resolves BEFORE the three console gates, so a synthetic id 404s from the binding rather than from the middleware a test names. | `M67` | 3455 | `bootstrap/app.php` |
| A line-splitting regex matches a byte INSIDE a UTF-8 character, and one faker name is enough to trigger it. | `M42` | 4036 | `tests/Feature/Audit/ImpersonationAttributionTest.php` · `docs/claims/lane-a.md` |
| `throttle:saml-acs`'s route BINDING is asserted by nothing, while its registration is. | `M30` | 3225 | `tests/Feature/Sso/SsoLoginWebTest.php` · `routes/tenant.php` · `app/Providers/AppServiceProvider.php` · `tests/Feature/Sso/SsoAcsWebTest.php` +1 |
| Twelve existing tenant-context call sites restore in a `finally` INSIDE their transaction, which is the shape `TenantContext::runFor()` was deliberately built … | `M3` | 708 | `app/Listeners/Auth/SendWelcomeEmail.php` · `app/Services/Admin/ImpersonationService.php` · `app/Services/Admin/SuperAdminService.php` · `app/Services/Auth/GoogleAuthRequestService.php` +3 |
| `lib/brand-cache.ts` is a SECOND writer to `guest-shell-html`, and it renews a mis-cased key with a response a navigation cannot use. | `M72` | 6008 | `resources/public-runtime/__tests__/brand-cache.test.ts` |
| The framework's own mail components interpolate into attributes the same way, and the M57 gate cannot reach them. | `M57` | 4537 | `scripts/mail-attribute-lint.php` · `resources/views/mail/notification.blade.php` |
| M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t… | `M5` | 810 | — *no file harvested* |
| A 5xx that arrives AFTER the provider committed is still re-driven. | `M5` | 823 | — *no file harvested* |
| A rotated token can still be lost in the one-UPDATE window M6 left. | `M6` | 875 | — *no file harvested* |
| `users.last_active_tenant_id` has no writer anywhere in `app/`. | `M8` | 2504 | — *no file harvested* |
| The database's uniqueness domain and the runtime's resolution domain now disagree about case, and `FormSlug::isTaken()` is the third resolver. | `M61` | 4790 | — *no file harvested* |
| `SubmissionFinalizer` is a SECOND writer of `submission_answers.form_version_id`, and it would erase the evidence of a divergence rather than record it. | `M73` | 6225 | — *no file harvested* |

## Not live — listed, not hidden

These are stated limits, deliberate decisions, blind spots and deployment obligations
rather than defects in this tree. `scripts/loop.php` already refuses them, and they are
listed here rather than dropped so that this file and that stop rule cannot disagree.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| M8's GRANT removed an accidental backstop that a mutation argument was leaning on. | `M8` | 2495 | *hub files only* |
| Two `WebhookRetrySweepTest` cases were passing for a reason unrelated to their names. | `M44` | 3858 | *hub files only* |
| The data dictionary names nine of the forty-five constraints the migrations declare, while enumerating constraints exhaustively in places. | `M46` | 4899 | *hub files only* |
| `scripts/tracker-lint-controls.php` proves R7 against synthetic histories, and nothing proves it against a REAL GitHub `push` or squash. | `M49` | 5537 | *hub files only* |
| The triage generator's collision check harvests citations from row TEXT, so it proposes batches that collide. | `M70` | 5740 | *hub files only* |
| Four P3a refusal cases assert the exception CLASS and never the message. | `M12` | 1904 | `tests/Feature/Submissions/SubmissionDraftServiceTest.php` |
| The delivery-rule modal's channel-refresh button is the same unguarded shape, GET-only. | `M23` | 2889 | `resources/js/components/integrations/RuleFormModal.vue` |
| A semantic token is no guarantee of a visible element, and one more instance is probably out there. | `M23` | 2909 | `resources/js/components/builder/LogicRail.vue` |
| Four `DO NOT RE-ASK` user decisions of record now live in `PROGRESS_ARCHIVE.md` rather than in `docs/claims/decisions.md`. | `M45` | 5264 | `docs/claims/decisions.md` |
| The citation-liveness gate cannot see a behaviour negative, and its ledger ceiling counts deliberately-preserved historical filings. | `M46` | 4907 | `docs/feature-backlog.md` |
| `routes/tenant.php`'s ~95 `can:` gates get none of M63's checks, and the derived half is expected to FIND something. | `M63` | 3567 | `routes/tenant.php` |
| `docs/backlog-triage.md` keeps a tier-1 citation exemption whose stated reason stopped being true in the same increment. | `M65` | 3669 | `scripts/citation-liveness-lint.php` |
| Any tenant-scoped policy read mounted on the Fortify group is silently blind, and two increments have now walked into it. | `M68` | 2590 | `config/fortify.php` |
| `gamification:backfill --dry-run` cannot reveal a mis-scoring defect, by construction. | `M24` | 2005 | `app/Services/Gamification/BackfillTally.php` · `app/Console/Commands/BackfillGamificationCommand.php` |
| Two SSO test files justify a real assertion with a rationale that is false on this framework version. | `M30` | 3235 | `tests/Feature/Sso/SsoLoginWebTest.php` · `tests/Feature/Sso/SsoLoginCompletionWebTest.php` |
| The liveness marker is gated for presence and nothing checks that a verdict is CORRECT — and the error rate of judging one is now measured rather than assumed. | `M65` | 3680 | `tests/Feature/Docs/BacklogProvenanceTest.php` · `scripts/loop.php` |
| Thirteen Vitest stubs across four files are silently inert. | `M23` | 2896 | `resources/js/Pages/submissions/show.test.ts` · `resources/js/components/sso/cards.test.ts` · `resources/js/components/sso/SsoPolicyCard.test.ts` · `resources/js/Layouts/AppLayout.test.ts` +2 |
| Every `MaintenanceJob` fan-out is proved one file at a time, so a future one inherits no coverage. | `M44` | 3869 | `tests/Feature/Queue/JobContractTest.php` · `app/Jobs/Maintenance/SweepWebhookRetriesJob.php` · `app/Jobs/Maintenance/SweepScheduledFormsJob.php` · `app/Jobs/Maintenance/RollUpUsageCountersJob.php` +5 |
| `replay.ts:223-228` hardcodes `conflict_code = 'form_updated'` on a client-side version guard. | `M14` | 1200 | `resources/public-runtime/lib/replay.ts` |
| The command gate reads `README.md` only, and three other documents carry runnable command blocks. | `M59` | 4748 | `docs/TESTING-GUIDE.md` · `docs/ACCESS-MATRIX.md` · `docs/deployment-infrastructure.md` |
| All sixteen synchronous dispatch listeners could now be `ShouldQueue`, and nothing has decided whether they should be. | `M3` | 695 | — *no file harvested* |
| Nothing checks that a `§D<n>` citation names a section whose text supports it. | `M7` | 5073 | — *no file harvested* |
| The sync surface's read and write are gated on different permission families, so no single non-admin role can complete the offline loop. | `M13` | 1848 | — *no file harvested* |
| the dashboard card ranks the whole tenant to compute three numbers that need no ranking. | `M26` | 2030 | — *no file harvested* |
| `loop assess` can only see what a row says about ITSELF, and two blind spots are now measured. | `M55` | 5022 | — *no file harvested* |
| A pre-existing mixed-case `public_slug` row would have been taken dark by M61, and nothing in the repository can tell whether one exists. | `M61` | 4801 | — *no file harvested* |

## Hub files — a batch may touch at most one

Derived, not listed: a file cited by **3 or more** open rows. These are
mostly meta-files rather than product code, which is why the rows that share them are not
really coupled — and why excluding them is what makes the queue above separable.

| File | Open rows citing it |
|---|---|
| `.github/workflows/ci.yml` | 11 |
| `scripts/state.php` | 7 |
| `scripts/backlog-triage.php` | 7 |
| `CLAUDE.md` | 5 |
| `docs/data-dictionary.md` | 4 |
| `docs/backlog-triage.md` | 4 |
| `composer.json` | 4 |
| `docs/security-threat-model.md` | 4 |
| `PROGRESS.md` | 3 |
| `PROGRESS_ARCHIVE.md` | 3 |
| `scripts/tracker-lint-controls.php` | 3 |
| `scripts/mutate.php` | 3 |
| `scripts/next.php` | 3 |
| `scripts/gate-baselines.php` | 3 |
| `docs/gate-baselines.md` | 3 |
| `scripts/pre-push-guard.php` | 3 |

## Suggested next batch

`D13` fixes the rule: **3–4 rows, no two citing the same non-hub file, at most one row touching a hub file.**
This is the greedy pick off the top of the queue above — a proposal to check, not a schedule.
A row whose files were not harvested cannot be checked for collision and is not proposed.

- **docs/feature-backlog.md:4113** — `M42` · *no non-hub file*
- **docs/feature-backlog.md:4611** — `M58` · `tests/Feature/Migrations/DocumentedDefaultDriftTest.php`
- **docs/feature-backlog.md:5705** — `M70` · `docs/audit-compliance-logging-spec.md`
- **docs/feature-backlog.md:5757** — `M71` · `scripts/tracker-surgery.php`

## What this file cannot see

**27 open row(s) cite no file this script could resolve**, and **13 carry at least one citation that does not resolve.** They are ranked below rows whose citations
all resolve, because a row must be re-derived before it is worked rather than merely worked —
but the ranking is the only consequence: an unresolvable citation is not evidence the row is
wrong. **Batch these by hand**, since a collision check needs files to compare.

Rows with no harvested file:

- `docs/feature-backlog.md:795` — A tabular rule that maps no Submission ID column still duplicates on an unconfirmed retry, and the durable fix is in the rule EDITOR rather than the adapter.
- `docs/feature-backlog.md:830` — `SlackConnector::deliver()` has the same non-idempotent shape and is deliberately not covered.
- `docs/feature-backlog.md:1859` — `promote()` re-asserts the version is published BEFORE the lock and never again under it.
- `docs/feature-backlog.md:1693` — The `reviewer` role's seeded description and `SubmissionPolicy::create()` contradict each other.
- `docs/feature-backlog.md:2323` — The tenant-facing SSO domains card on `/settings/sso` does not exist, so verification is operator-assisted.
- `docs/feature-backlog.md:2338` — `MemberController::invite()` validates `['required', 'email', 'max:255']` and a role, with no domain-ownership check.
- `docs/feature-backlog.md:2349` — Self-registration remains a way to occupy an address in a domain you do not control.
- `docs/feature-backlog.md:1381` — The storage-quota line counts strangers' submissions.
- `docs/feature-backlog.md:4005` — Every hand-off prescribes a Pint command that scans ~40 fewer files than CI does.
- `docs/feature-backlog.md:3209` — `PUT /user/profile-information` is a second mail cannon, and it is the one Fortify write route left deliberately unbound.
- `docs/feature-backlog.md:1888` — The promote route still documents neither of its two 403 causes nor its 422, and the generator cannot narrow a refusal family to one route.
- `docs/feature-backlog.md:1783` — The per-item `error.code` is the integrator's real branching key on `/sync/submissions`, and the contract still publishes it as an unconstrained string.
- `docs/feature-backlog.md:6085` — `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index …
- `docs/feature-backlog.md:6329` — What a media cell should SAY beyond a filename, and what shape a grid answer takes in a spreadsheet, are two product questions `M74` deliberately did not answe…
- `docs/feature-backlog.md:6546` — The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s…
- `docs/feature-backlog.md:810` — M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t…
- `docs/feature-backlog.md:823` — A 5xx that arrives AFTER the provider committed is still re-driven.
- `docs/feature-backlog.md:875` — A rotated token can still be lost in the one-UPDATE window M6 left.
- `docs/feature-backlog.md:2504` — `users.last_active_tenant_id` has no writer anywhere in `app/`.
- `docs/feature-backlog.md:4790` — The database's uniqueness domain and the runtime's resolution domain now disagree about case, and `FormSlug::isTaken()` is the third resolver.
- `docs/feature-backlog.md:6225` — `SubmissionFinalizer` is a SECOND writer of `submission_answers.form_version_id`, and it would erase the evidence of a divergence rather than record it.
- `docs/feature-backlog.md:695` — All sixteen synchronous dispatch listeners could now be `ShouldQueue`, and nothing has decided whether they should be.
- `docs/feature-backlog.md:5073` — Nothing checks that a `§D<n>` citation names a section whose text supports it.
- `docs/feature-backlog.md:1848` — The sync surface's read and write are gated on different permission families, so no single non-admin role can complete the offline loop.
- `docs/feature-backlog.md:2030` — the dashboard card ranks the whole tenant to compute three numbers that need no ranking.
- `docs/feature-backlog.md:5022` — `loop assess` can only see what a row says about ITSELF, and two blind spots are now measured.
- `docs/feature-backlog.md:4801` — A pre-existing mixed-case `public_slug` row would have been taken dark by M61, and nothing in the repository can tell whether one exists.
