# Backlog triage

**Generated by `scripts/backlog-triage.php`. Do not hand-edit — regenerate it.**

**Measured 2026-09-18** against `origin/main` at `99d02232e71b7280be8d66008eb105445a085671` · 233 open rows · 1 `major` · 232 `minor` · 0 `nit`

⚠️ **This is an OPERABILITY order, not a priority order, and the difference is why the file it
replaced went wrong.** It cannot tell you what matters most. Severity would, and there are zero
open `major` rows, so that field separates nothing. What it derives is which rows are still
**live**, whose **citations still resolve**, and which rows **share no files** with each other —
which is what picking a batch needs, and is all this file claims.

⛔ **Liveness here is a marker a human wrote, not a measurement this script made.** It is gated
for presence by `tests/Feature/Docs/BacklogProvenanceTest.php` and reported by
`scripts/state.php`; neither checks that a verdict is *right*. A row marked live that is dead
passes everything. The mutation harness is what settles a row, and settling it is the job of
whichever increment takes it.

📌 **The 2026-08-28 census this file replaced is preserved byte-for-byte in `docs/backlog-triage-m37.md`.**
Its findings about the sweep's own accuracy, and the brief it ran under, are not reproduced here
and are not stale — they are a dated record and are still worth reading before any fan-out.

---

## Census

| | Rows |
|---|---|
| Open | **233** |
| — still `live` | **154** |
| — `latent`, needs a stated precondition | 61 |
| — `not live`, not a defect in this tree | 18 |
| Severity bullets, ever | 444 |

## The queue — live rows, most operable first

Ordered by citation health, then by how few non-hub files the row touches, then oldest
debt first. `cites` lists only the files a batch would collide on; hub files are excluded
from it and listed separately below.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| `submission_geo_index` is a real table and the data dictionary does not mention it at all. | `M58` | 4775 | *hub files only* |
| The triage generator silently drops any citation written as a PARTIAL path, and a partial path is strictly worse than a bare filename. | `M73` | 6462 | *hub files only* |
| The standing `pgsql_auth` rule was never literally true, and its stated revisit trigger names a different grant. | `M78` | 7410 | *hub files only* |
| `scripts/loop.php --assess` does not read `docs/pipeline.md`, so it cannot refuse a row on its PIPELINE state. | `M79` | 5979 | *hub files only* |
| The `pipeline-lint` controls mirror the LIVE corpus, so a defect in one of the four coverage predicates reddens all 38 cases and drowns the control that names … | `M82` | 7931 | *hub files only* |
| Two PRD acceptance criteria are dispositioned in INDENTED CONTINUATION bullets the collector skips, so `P2e`'s residue reconciles against its own predicate rat… | `M84` | 8332 | *hub files only* |
| `docs/ACCESS-MATRIX.md` cites a HOSTNAME AND PORT, which the citation pattern parses as a path and line. | `M85` | 8430 | *hub files only* |
| `LEDGER_ROT_CEILING` cannot ratchet down while preserved original filings are counted, and `M85` has now paid that in the other direction. | `M85` | 8450 | *hub files only* |
| The generated queue's ORDER depends on which UNTRACKED directories happen to exist, and `docs/pipeline.md` is merge-gated on that order. | `M85` | 8461 | *hub files only* |
| `scripts/tracker-lint.php` is cited by ZERO of the open rows while owning the defects of at least two of them, so no collision check can ever see it. | `M86` | 8510 | *hub files only* |
| Eleven value-domain `CHECK` constraints guard columns the enum catalog has no row for at all, and the fix cannot be made while the citation ledger is at its ce… | `M87` | 8648 | *hub files only* |
| `P2d`'s literal-`null` blind spot hides three genuinely inert columns, and the honest repair needs the plumbing its own row says it does not. | `M89` | 9043 | *hub files only* |
| `scripts/` is the one tree `test-pointer-lint` still cannot scan, and it cannot become a root while the gate's own header is written the way it is. | `M92` | 9406 | *hub files only* |
| `docs/data-dictionary.md` holds a fourth copy of `bot_challenge` in prose and `FormBotChallenge` is in no catalog row, and `M92` could not take it. | `M92` | 9487 | *hub files only* |
| A gate's SUMMARY LINE is a paired artefact across three files, and nothing declares that pairing at the place where somebody edits one. | `M92` | 9513 | *hub files only* |
| Nobody who loses their two-step sign-in device and their recovery codes can get back in without an operator editing the database. | `M93` | 9603 | *hub files only* |
| A struck closure records no closer. | `M95` | 9855 | *hub files only* |
| ADR-0007 points at rows of ADR-0005 by bare line number, and every pointer lands on the wrong row. | `M95` | 9881 | *hub files only* |
| The Next section and the hand-off list open decisions as work to take. | `M96` | 9915 | *hub files only* |
| ADR-0007 points at the runbook by bare line numbers that have moved. | `M96` | 9986 | *hub files only* |
| The design-system reference describes filter chips, a clear-all action and width-matched skeleton cells that no list renders. | `M96` | 10018 | *hub files only* |
| The invitation's sending domain publishes no DMARC and no aligned DKIM, and an emailed invitation is the only way a tester gets an account. | `M98` | 10171 | *hub files only* |
| The ledger's header sends new work to a section no row has been filed under since `M93`. | `M98` | 10305 | *hub files only* |
| The first-boot runbook and the checklist set up none of the database backup §5 prescribes. | `M98` | 10412 | *hub files only* |
| `R7`'s byte threshold was calibrated on surgeries that were all left too late, so acting EARLY lands in its dead zone. | `M71` | 5983 | `scripts/tracker-surgery.php` |
| What actually delivers the offline mis-cased render is unknown, and TWO confident models of it have now been wrong. | `M73` | 6390 | `tests/e2e/public-runtime-offline.spec.ts` |
| The triage generator harvested a row as hub-free whose repair could only be made in a hub file, and the batch it then proposed violated `D13`. | `M83` | 8169 | `tests/Feature/Migrations/DocumentedDefaultDriftTest.php` |
| `scripts/mutate.php` cannot prove ANY gate whose truthful container answer is RED, and a shipped gate is already in that state. | `M84` | 8267 | `tests/Feature/Docs/SuiteCollectionFloorTest.php` |
| The archived status bullets live in TWO non-contiguous places, and that is what made the batch numbering go wrong. | `M86` | 8560 | `scripts/tracker-surgery.php` |
| No control harness drives the real `scripts/backlog-triage.php` or `scripts/pipeline.php`, and the one harness that looks like it does stubs the generator. | `M86` | 8573 | `tests/Feature/Docs/PipelineLintControlsTest.php` |
| The optimistic-concurrency token §8 substituted for the draft lock is CLIENT-OPTIONAL, and a shipped test pins it that way. | `M88` | 8827 | `docs/form-versioning-schema-migration.md` |
| Three client-side workarounds were built ON the JSON-refusal defect `M90` fixed, and all three can now be simplified — but one of them is load-bearing until it… | `M90` | 9220 | `docs/gamification-design.md` |
| A third instance of the citation class `M83` filed in the abstract: three live lines that say nothing about what cites them. | `M91` | 9352 | `app/Services/Forms/FormBuilderService.php` |
| `MIRROR_DIVERGENCES` is keyed by mirror NAME alone, so two same-named mirrors in different files would silently share one exception. | `M92` | 9418 | `tests/Feature/Docs/DocumentedEnumMirrorDriftTest.php` |
| Three comments corrected by `M92` now name an arm that asserts LESS than they claim, and the gap is different in each. | `M92` | 9474 | `app/Models/Domain.php` |
| Four super-admin console pages have no axe scan: `tests/e2e/admin-console-axe.spec.ts` covers only the settings and feedback pages. | `M93` | 9597 | `tests/e2e/admin-console-axe.spec.ts` |
| The pre-push guard's protocol paths omit `docs/pipeline.md`. | `M95` | 9862 | `scripts/pre-push-guard.php` |
| PHP's file-size limits in the dev image reject attachments the app allows. | `M95` | 9873 | `config/attachments.php` |
| Two user-supplied predicates now run on the `pgsql_auth` connection, against a standing rule that says none may. | `M77` | 7025 | `docs/multi-tenancy-rbac-design.md` · `docs/adr/0002-multi-tenancy-shared-db-rls.md` |
| §7.1's `Form draft` row pins four `/api/v1` builder endpoints registered nowhere, and the `validations` sub-resource exists on neither surface. | `M80` | 7530 | `docs/xlsform-interop-spec.md` · `app/Services/Forms/FormBuilderService.php` |
| The API rate-limit table promises 300 requests/minute per authenticated user and no such limiter is defined. | `M80` | 7726 | `app/Providers/FortifyServiceProvider.php` · `app/Providers/QueueServiceProvider.php` |
| A `ControlKind` docblock claims it is derived exactly as the SFC's own `control` computed, the two disagree in three ways, and no gate can compare them. | `M92` | 9440 | `resources/public-runtime/lib/types.ts` · `resources/js/components/submissions/FieldInput.vue` |
| The central landing page offers "Create a workspace", and the account it creates has no workspace and lands on a 404. | `M93` | 9576 | `resources/js/Pages/Welcome.vue` · `tests/e2e/auth-axe.spec.ts` |
| The audit spec's §1 table is asserted by nothing, and a static sweep cannot be the thing that asserts it. | `M70` | 5896 | `docs/audit-compliance-logging-spec.md` · `app/Services/Gamification/AuditReplayMap.php` · `docs/adr/0020-gamification-awarding-substrate.md` |
| `forms.allow_manual_encoding` is documented as Feature #7's capability flag and has neither a reader nor a writer — the only one of five inert `allow_*` flags … | `M80` | 7450 | `tests/Feature/Tenancy/TenantExtractColumnDriftTest.php` · `app/Policies/SubmissionPolicy.php` · `app/Services/Forms/FormService.php` |
| "1 concurrent sync export per form, additional requests 429" — no concurrency guard exists on any export path. | `M80` | 7796 | `app/Jobs/Connectors/RefreshOneConnectionJob.php` · `app/Jobs/MaintenanceJob.php` · `app/Services/Submissions/FormAcceptanceGuard.php` |
| `scripts/pipeline-lint.php` P2d recognises a cast by a CLOSED VOCABULARY of cast names, so a cast this project adopts later reads as a write and silently un-do… | `M81` | 7874 | `scripts/pipeline-lint.php` · `app/Services/Sso/SsoDomainService.php` · `app/Services/Attachments/AttachmentStorageService.php` |
| Every handled tenancy-identification failure is logged at ERROR with a full stack trace. | `M98` | 10335 | `app/Http/Middleware/InitializeTenancyByPublicHost.php` · `app/Models/Domain.php` · `tests/Feature/Tenancy/CentralHostFallbackTest.php` |
| The documented `Users & roles` API resource group (`GET/POST /api/v1/users`, `/api/v1/roles`) has zero routes, and a shipped schema decision was already paid f… | `M80` | 7513 | `app/Http/Resources/Api/V1/AuditResource.php` · `docs/multi-tenancy-rbac-design.md` · `app/Models/Role.php` · `config/permission.php` |
| Every visitor reaches the testing server as one private address, so the per-address limits, the login lockout and every audit IP lose their meaning. | `M98` | 10149 | `config/guest.php` · `app/Providers/FortifyServiceProvider.php` · `app/Http/Middleware/EnforceGuestFormRateLimit.php` · `app/Support/Audit/AuditLogger.php` |
| The welcome email is sent again after every verified address change. | `M98` | 10458 | `app/Listeners/Auth/SendWelcomeEmail.php` · `app/Actions/Fortify/UpdateUserProfileInformation.php` · `resources/js/Pages/Settings/Index.vue` · `tests/Feature/Auth/WelcomeEmailTest.php` |
| The schedule-acceptance vocabulary has four TypeScript copies and a PHP side that is raw strings rather than an enum, so nothing can ever gate it. | `M92` | 9451 | `resources/public-runtime/lib/types.ts` · `resources/js/Pages/forms/Show.vue` · `resources/js/Pages/submissions/Encode.vue` · `resources/js/Pages/forms/show.test.ts` +2 |
| The platform console's Save reports success identically whether or not anything changed. | `M98` | 10354 | `app/Http/Controllers/Admin/PlatformSettingsController.php` · `resources/js/Pages/admin/Settings.vue` · `packages/design-system/src/components/Switch/Switch.vue` · `app/Http/Requests/Admin/UpdatePlatformSettingsRequest.php` +2 |
| The runbook and checklist steer the testing workspace onto Starter, whose caps bind with no billing. | `M98` | 10391 | `database/seeders/Data/PlanCatalog.php` · `app/Services/Entitlements/QuotaGuard.php` · `app/Services/Entitlements/EntitlementService.php` · `app/Services/Tenancy/TenantMembershipService.php` +2 |
| NFR §8 sets a 30-day soft-delete grace period before a hard-deletion job, and for the three entities it names there is no purge job, no grace-period config val… | `M80` | 7617 | `app/Jobs/Maintenance/PruneFailedJobsJob.php` · `app/Jobs/Submissions/ReapTenantDraftsJob.php` · `app/Services/Submissions/ClientUuidResolver.php` · `app/Policies/FormPolicy.php` +4 |
| `forms.allow_offline_sync` has no reader anywhere — neither the sync manifest nor the PWA install entry honours the per-form offline gate that two documents de… | `M80` | 7472 | `tests/Feature/Tenancy/TenantExtractColumnDriftTest.php` · `docs/erd.md` · `docs/ux/form-filling-ux-flow.md` · `app/Http/Controllers/Api/V1/SyncManifestController.php` +5 |
| `scripts/mutate.php` cannot drive a positive control for anything that is not Pest in a container. | `M42` | 4367 | *hub files only* |
| A tracker surgery whose diff touches only `paths-ignore`d files produces no post-merge run at all, so `R7`'s trunk arm is unreachable for exactly the diff shap… | `M48` | 5498 | *hub files only* |
| `scripts/backlog-triage.php --check` is wired into nothing, so the generated triage can drift with no gate saying so. | `M65` | 3795 | *hub files only* |
| `docs/backlog-triage.md` is generated stale by its own close-out, and the drift is on the trunk now. | `M71` | 6087 | *hub files only* |
| `docs/data-dictionary.md` documents nine `tenants` columns that exist in no migration, and its enum catalog contradicts the enum. | `M72` | 6308 | *hub files only* |
| `scripts/next.php` never clips the release HEADING, so a third of the hand-off is byte-identical noise. | `M72` | 6359 | *hub files only* |
| `CLAUDE.md`'s gate table sends PHPStan to the container, one row below the rule that explains why the container is wrong. | `M76` | 6899 | *hub files only* |
| The partial-path citation row's consequences are mostly false, and the measurement that settles `D15`'s dependency is done. | `M76` | 6982 | *hub files only* |
| Every close-out drifts `docs/pipeline.md`, because a status bullet added to `PROGRESS.md` shifts the two markers at its end and the generated line cites them b… | `M82` | 7987 | *hub files only* |
| `pipeline.php --check` compares the file only from `## The line` onward, so the BANNER — the one part a human would hand-correct, and the part `state.php` read… | `M82` | 8015 | *hub files only* |
| The citation gate asserts a cited line is ALIVE, never that it still says what the citation claims — so re-pointing a shifted citation arithmetically merges gr… | `M83` | 8049 | *hub files only* |
| A `FormRequest` class docblock is PUBLISHED API DOCUMENTATION, and nothing says so at the place where someone writes one. | `M92` | 9497 | *hub files only* |
| Seventeen line pointers into this ledger resolve to unrelated lines, and no gate reads them. | `M94` | 9730 | *hub files only* |
| The runbook misses four Windows host prerequisites that stop a first boot or a deploy on the testing server. | `M97` | 10071 | *hub files only* |
| `backlog-triage.php` harvests no `.ps1` path, so `deploy.ps1` has no edge in the `D13` graph. | `M98` | 10291 | *hub files only* |
| PROGRESS.md is within about five close-outs of the tracker-lint byte ceiling. | `M98` | 10470 | *hub files only* |
| The one sentence a struck row left behind — never approve an unknown contributor's run — is assigned to two rows disjunctively and carried by neither. | `M99` | 10481 | *hub files only* |
| A claim file has no constrained form for a forward declaration, so the one stale declaration on the tree cannot be gated. | `M42` | 4240 | `docs/claims/lane-b.md` |
| `tracker-lint` R8 guards `CLAUDE.md` and cannot reach `PROGRESS.md`, which is the half that actually rotted. | `M42` | 4252 | `scripts/tracker-lint-controls.php` |
| `scripts/next.php` takes each release's LEAD paragraph, and a lead paragraph is often a file manifest rather than the lesson. | `M42` | 4387 | `docs/claims/TEMPLATE.md` |
| `docker compose run --rm e2e` does not work on this host as three documents prescribe it, and the most likely wrong form EXITS 0. | `M61` | 4996 | `docs/ux/exceptions-log.md` |
| PHPUnit's own collector loses 40 test files in the container, and a local run reports green without them. | `M76` | 6928 | `tests/Feature/Docs/SuiteCollectionFloorTest.php` |
| The storage-quota row's stated blocker is not real, and its re-aim needs a copy decision nobody has made. | `M77` | 7143 | `docs/adr/0021-respondent-scoped-device-outbox.md` |
| `public-runtime-offline.spec.ts`'s parked-conflict case is FLAKY across viewport projects, and `D2` makes that a merge-blocking property. | `M77` | 7183 | `resources/public-runtime/sw.ts` |
| §7.1 names a `/api/v1/webhooks/endpoints` path segment that exists nowhere in the tree, and it is a floor rather than a census. | `M80` | 7600 | `tests/Feature/Webhooks/WebhookEndpointApiTest.php` |
| `scripts/citation-liveness-lint.php` sits AT its ceiling with zero headroom, so any edit that shifts a line in `docs/data-dictionary.md` is a merge failure and… | `M83` | 8033 | `docs/adr/0008-entitlement-and-metering.md` |
| A `docs/feature-backlog.md` citation into the exceptions log resolves to a live line about a different subject, and the citation gate is built to pass exactly … | `M87` | 8711 | `docs/ux/exceptions-log.md` |
| `D13`'s one-hub-row cap cannot admit a new lint gate at all, and `M90` broke it for exactly that reason. | `M90` | 9232 | `.github/workflows/ci.yml` |
| The welcome email tells a central-host account to create a workspace. | `M95` | 9755 | `tests/Feature/Auth/WelcomeEmailTest.php` |
| `TopNav.vue` says the tenant switcher does not exist yet, and nothing queues it. | `M96` | 9980 | `resources/js/components/shell/TopNav.vue` |
| `buildRows`'s docblock argues against the heading match, and one test title gives a reason that no longer holds. | `M96` | 10035 | `resources/js/components/integrations/mapping-model.ts` |
| The first-boot runbook describes only nginx behind a wildcard domain, and the testing server runs Apache with one workspace on an existing address. | `M97` | 10062 | `tests/Feature/Mail/QueuedMailContractTest.php` |
| The first-boot steps leave `LOG_LEVEL=debug` and one unrotated log file in place. | `M98` | 10426 | `config/logging.php` |
| `D13`'s file-overlap rule cannot be satisfied by any pair of rows, because the generated hub set now contains product code. | `M99` | 10522 | `public/index.php` |
| The service worker caches a credential-bearing resume shell, and only the RENEWAL of it was closed. | `M70` | 5850 | `tests/e2e/public-runtime-offline.spec.ts` · `docs/offline-first-sync-design.md` |
| No gate whose input is the GIT INDEX can have a Pest control, because `git` is not installed in the app container. | `M85` | 8403 | `scripts/tracker-lint-controls.php` · `scripts/citation-liveness-lint-controls.php` |
| The `docs/claims/ | `M85` | 8441 | `docs/claims/lane-b.md` · `resources/public-runtime/lib/db.ts` |
| Two more scripts have `backlog-triage.php`'s destructive default, and the row that filed that defect names one of them as its harmless counter-example. | `M71` | 6041 | `scripts/regenerate-brand-ramp-fixture.php` · `tests/fixtures/brand-ramp.json` · `scripts/npm-audit-judge.php` |
| The documented async export API — `POST /api/v1/forms/{form}/exports` and `GET /api/v1/exports/{export}` — has zero routes and no `exports` job row, while the … | `M80` | 7493 | `app/Enums/QueueName.php` · `app/Jobs/Submissions/GeneratePdfJob.php` · `config/queue-fairness.php` |
| `docs/api-specification.md:63` states in the present tense that every unsafe request is deduplicated against a 24-hour Redis cache keyed on `(tenant_id, endpoi… | `M80` | 7646 | `app/Services/Submissions/SubmissionPipeline.php` · `app/Services/Webhooks/WebhookEventDispatcher.php` · `app/Services/Gamification/PointsRecorder.php` |
| `backlog-triage.php` harvests a fraction of some rows' paths, so `D13`'s overlap grouping is computed from an undercount. | `M99` | 10560 | `resources/views/deploy-window.blade.php` · `public/index.php` · `vendor/autoload.php` |
| `scripts/pipeline-lint.php`'s four coverage rules read the MARKDOWN half of the corpus only, and three deferral sentences live in the PHP half where nothing ca… | `M82` | 7890 | `scripts/pipeline-lint.php` · `app/Enums/AnalyticsAxis.php` · `app/Enums/FormBotChallenge.php` · `app/Services/Submissions/SubmissionInboxPresenter.php` |
| §7.1's flat webhook-delivery paths and §7.4's per-tenant delivery-log UI both contradict the same document's per-endpoint spec, and only the per-endpoint form … | `M80` | 7576 | `app/Http/Controllers/Api/V1/WebhookDeliveryController.php` · `app/Services/Webhooks/WebhookEndpointPresenter.php` · `resources/js/Pages/webhooks/Index.vue` · `app/Enums/WebhookDeliveryStatus.php` +1 |
| A cluster of by-line citations went stale, several of them inside this branch. | `M1` | 5240 | `config/queue.php` · `config/app.php` · `docs/audit-compliance-logging-spec.md` · `docs/piping-output-encoding-design.md` +2 |
| `forms.single_page_mode` has no write surface outside the seeders, so single-page mode is unreachable for a real tenant — and its documented default disagrees … | `M80` | 7428 | `app/Services/Submissions/EncodeFormPresenter.php` · `app/Services/Submissions/PublicFormPresenter.php` · `database/seeders/DemoSeeder.php` · `database/seeders/E2eSeeder.php` +5 |
| The parity matrix scores API/programmatic import as Phase-1 shipped, and the code's own enum docblock calls it a later channel. | `M80` | 7551 | `docs/competitive-feature-parity-matrix.md` · `app/Services/Submissions/SubmissionDraftService.php` · `app/Http/Controllers/Api/V1/SyncSubmissionController.php` · `app/Http/Controllers/Public/GuestDraftController.php` +6 |
| `SlackConnector::deliver()` has the same non-idempotent shape and is deliberately not covered. | `M5` | 856 | — *no file harvested* |
| The tenant-facing SSO domains card on `/settings/sso` does not exist, so verification is operator-assisted. | `M18` | 2425 | — *no file harvested* |
| `MemberController::invite()` validates `['required', 'email', 'max:255']` and a role, with no domain-ownership check. | `M18` | 2440 | — *no file harvested* |
| Self-registration remains a way to occupy an address in a domain you do not control. | `M18` | 2451 | — *no file harvested* |
| `MdsSegmentedControl` spills 30px INSIDE the builder's config pane — a real horizontal scrollbar, and not the page-overflow defect it was filed as. | `M19` | 630 | — *no file harvested* |
| The storage-quota line counts strangers' submissions. | `M21` | 1407 | — *no file harvested* |
| Three gate invocations fetch `http-server` from the network at run time, and nothing declares it. | `M59` | 4894 | — *no file harvested* |
| The promote route still documents neither of its two 403 causes nor its 422, and the generator cannot narrow a refusal family to one route. | `M67` | 1990 | — *no file harvested* |
| The per-item `error.code` is the integrator's real branching key on `/sync/submissions`, and the contract still publishes it as an unconstrained string. | `M69` | 1835 | — *no file harvested* |
| `M59`'s undeclared-`http-server` row undercounts on both of its numbers. | `M71` | 6134 | — *no file harvested* |
| `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index … | `M72` | 6342 | — *no file harvested* |
| The `npm audit` judge makes a REQUIRED context green while nothing was measured, and that is a deliberate trade rather than an oversight. | `M72` | 6376 | — *no file harvested* |
| What a media cell should SAY beyond a filename, and what shape a grid answer takes in a spreadsheet, are two product questions `M74` deliberately did not answe… | `M74` | 6607 | — *no file harvested* |
| The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s… | `M75` | 6824 | — *no file harvested* |
| A correction still cannot be autosaved, and the reason is an endpoint that does not exist plus a product decision nobody has taken. | `M75` | 6883 | — *no file harvested* |
| `forms.timezone` is documented as defaulting to NULL and the database defaults it to `'UTC'`. | `M78` | 7315 | — *no file harvested* |
| Two columns are documented under the wrong table in the data dictionary. | `M78` | 7322 | — *no file harvested* |
| Nine live tables have no dictionary section, and they are not all framework scaffolding. | `M78` | 7329 | — *no file harvested* |
| A pipeline marker placed mid-document SILENTLY INVALIDATES every `path:N` citation beneath it, and only the ones that land on a blank line are caught. | `M79` | 5981 | — *no file harvested* |
| Six `Default` cells describe a column as application-supplied when the database does supply a default, and one of the two words used is factually wrong. | `M83` | 8098 | — *no file harvested* |
| Three `tenants` cells disagree with the live schema on Type and Nullable, and no gate compares either column. | `M83` | 8110 | — *no file harvested* |
| `promote()`'s pre-lock shape has two more instances, and one of them is 74 lines above the row that named it. | `M85` | 8347 | — *no file harvested* |
| Four citations point into UNTRACKED trees and can never resolve, and nothing separates them from citations that are merely broken. | `M85` | 8418 | — *no file harvested* |
| `.sheets-fields` is probably the worst `MdsSegmentedControl` host in the tree and it is the one with no coverage of any kind. | `M87` | 8699 | — *no file harvested* |
| Three more lock-holding transitions DELETE the child rows a concurrent builder edit is writing, and for all three the RLS backstop does not fire. | `M88` | 8837 | — *no file harvested* |
| `FormService` has seven lock-free writers to `forms` and one locked, and six of them build their audit payload from a model nobody re-read. | `M88` | 8847 | — *no file harvested* |
| One `forceFill` writes `closes_at` and `max_responses` together, and the acceptance guard treats one as authoritative-under-lock and the other as ignorable-pre… | `M88` | 8858 | — *no file harvested* |
| Standing Rule 7(b-bis) says "Three files" against a table of five rows, inside the rule that exists to keep paired files in step. | `M91` | 9326 | — *no file harvested* |
| The mirror gate's comment stripper is line-based and unscoped to `<script>`, so its property grammar on a `.vue` file is first-match-wins across the template a… | `M92` | 9462 | — *no file harvested* |
| In the builder at compact widths, selecting a field does not bring its settings on screen. | `M94` | 9651 | — *no file harvested* |
| The builder's side-pane widths are fixed pixels, so at the largest text size the pane-switch threshold no longer matches the columns it is derived from. | `M94` | 9657 | — *no file harvested* |
| A later successful builder save clears the unsaved warning for an earlier field that still failed. | `M94` | 9662 | — *no file harvested* |
| A repeated identical builder save failure is announced to a screen reader only once. | `M94` | 9668 | — *no file harvested* |
| The builder toolbar says "All changes saved" on a form that cannot be edited. | `M94` | 9672 | — *no file harvested* |
| The builder's "Not saved" status looks the same as "All changes saved". | `M94` | 9676 | — *no file harvested* |
| Discarding a conflicted offline response uses the browser's plain confirm box. | `M94` | 9691 | — *no file harvested* |
| In an installed offline form, opening another form's queued response leaves the installed window. | `M94` | 9696 | — *no file harvested* |
| Reviewing an offline conflict reopens a form that has since closed or filled up, and the resubmit is then refused. | `M94` | 9701 | — *no file harvested* |
| Two accessibility specs still scan only the open dialog rather than the whole page. | `M94` | 9705 | — *no file harvested* |
| The password-strength requirement list loses its list semantics in Safari, and 21 more files carry the same shape. | `M94` | 9724 | — *no file harvested* |
| The token refresh re-check cannot stop a redundant rotation. | `M95` | 9783 | — *no file harvested* |
| A connector test send has no token pre-flight. | `M95` | 9792 | — *no file harvested* |
| Operator commands have no well-formed audit shape. | `M95` | 9809 | — *no file harvested* |
| The queue enum's docblock says five names. | `M95` | 9869 | — *no file harvested* |
| A deploy deletes the previous build's chunks, so an open tab can fail to load a script it needs. | `M96` | 9931 | — *no file harvested* |
| The inbox orders a promoted draft by when it was started, not when it was submitted. | `M96` | 9963 | — *no file harvested* |
| Seven list pages hand-roll the same one-line hint above a table. | `M96` | 9999 | — *no file harvested* |
| The `email` rule accepts a local part longer than 64 characters. | `M96` | 10005 | — *no file harvested* |
| Two delivery logs page with no loading state. | `M96` | 10024 | — *no file harvested* |
| Pressing Check on a Sheets rule whose tab is not the first re-reads the first tab and drops the rule's bindings. | `M96` | 10029 | — *no file harvested* |
| The `md-status` check the operator is told to read returns a Laravel page, because the front-controller rewrite swallows it. | `M99` | 10503 | — *no file harvested* |
| The member roster 500s when a membership names a user the query cannot return. | `M99` | 10549 | — *no file harvested* |

## Latent — real, but each needs a stated precondition first

Not queue work until the precondition holds. The row says what it is.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| A verified SSO email domain is trusted indefinitely — there is no re-verification sweep. | `M18` | 2410 | *hub files only* |
| Route-model binding resolves BEFORE the three console gates, so a synthetic id 404s from the binding rather than from the middleware a test names. | `M67` | 3594 | *hub files only* |
| Two more callers of the snapshot serializer still read a torn tree, and whether they should follow `M90`'s instrument is an open decision rather than an oversi… | `M90` | 9184 | *hub files only* |
| The super-admin console has no network-level restriction, and hardening it is recorded as deferred with no row. | `M93` | 9591 | *hub files only* |
| An SSO sign-in whose provisioning hits a missing role is answered with a redirect back towards the identity provider rather than the uniform refusal. | `M93` | 9609 | *hub files only* |
| The SSO step-up escape link that `docs/security-threat-model.md` §9 records as owed was never added. | `M93` | 9616 | *hub files only* |
| The expression engine bounds parse depth, memo size, length and token count, and has no evaluation-time budget. | `M93` | 9620 | *hub files only* |
| A webhook retry or redeliver is signed with the event's original time, so a receiver that enforces a five-minute replay window rejects every attempt sent after… | `M94` | 9625 | *hub files only* |
| The forms list loads every form a member can see in one request, with no pagination. | `M94` | 9634 | *hub files only* |
| `queue:work --timeout` cannot fire on Windows. | `M95` | 9774 | *hub files only* |
| No path revokes `is_super_admin`. | `M95` | 9819 | *hub files only* |
| A deploy that fails before the site comes back up leaves a copy of the server's `.env` in the staging worktree. | `M96` | 10042 | *hub files only* |
| The checklist creates the app, PHP, NSSM and runner folders directly under `C:\` and never restricts their permissions. | `M98` | 10188 | *hub files only* |
| The deploy runner holds Modify on the tree that SYSTEM services execute, and any NETWORK SERVICE process can control the queue worker. | `M98` | 10204 | *hub files only* |
| The runbook's custom-domain arm prescribes nginx, IIS and an HTTP-01 challenge on a box that runs Apache with port 80 closed. | `M98` | 10218 | *hub files only* |
| `baselineOf()` turns "no checksum" into `''`, and only middleware turns it back. | `M31` | 3106 | `tests/Feature/Submissions/SubmissionEditRoutesTest.php` |
| A line-splitting regex matches a byte INSIDE a UTF-8 character, and one faker name is enough to trigger it. | `M42` | 4175 | `tests/Feature/Audit/ImpersonationAttributionTest.php` |
| ADR-0007 §D11 describes three queue connections that `config/queue.php` does not define, and asserts they are annotated as forbidden when they were deleted. | `M80` | 7760 | `config/queue.php` |
| The documented guest per-IP rate limit of 100/min has no definition — the ceiling on the surface that row describes is 60. | `M80` | 7778 | `config/guest.php` |
| `BlueprintValidator` enforces no key FORMAT at all, and `SchemaTreeCloner` carries keys forward verbatim — defence in depth rather than a live defect, and it s… | `M91` | 9337 | `app/Services/Forms/BlueprintValidator.php` |
| The central-host redirect is an absolute `APP_URL`, which a request from another origin cannot follow. | `M96` | 9922 | `tests/e2e/global-setup.ts` |
| The deploy workflow ships `origin/main`, not the commit whose CI run triggered it. | `M98` | 10132 | `.github/workflows/deploy.yml` |
| Google sign-in cannot work on the testing layout, because its redirect URI is built from `APP_URL`, which is the agency's website. | `M98` | 10229 | `config/services.php` |
| The pre-push guard's claim rule is a substring test, so a push from a detached HEAD passes it. | `M98` | 10278 | `scripts/pre-push-guard.php` |
| Structured JSON application logs are documented in the present tense and `config/logging.php` has no JSON formatter on any channel. | `M80` | 7695 | `config/logging.php` · `docs/observability-incident-response.md` |
| `export_artifact` objects are documented as auto-deleted seven days after generation, and no scheduled cleanup task is declared. | `M80` | 7747 | `app/Enums/AttachmentKind.php` · `app/Services/Submissions/SubmissionPdfStorage.php` |
| 26 foreign keys can point at another workspace's rows, and 20 of them would cascade a delete across workspaces. | `M94` | 9710 | `docs/adr/0002-multi-tenancy-shared-db-rls.md` · `scripts/constraint-boundary-lint.php` |
| Two more middlewares drop a non-GET payload through `Redirector::guest`, and one of them records no intended URL at all. | `M99` | 10535 | `app/Http/Middleware/EnsureVerifiedEmail.php` · `app/Http/Middleware/EnsureSuperAdminMfa.php` |
| Correlation IDs threaded through every log line — `request_id` and `job_chain_id` — have no mechanism at all. | `M80` | 7711 | `docs/observability-incident-response.md` · `app/Models/SsoAuthRequest.php` · `app/Services/Sso/SsoAuthRequestService.php` · `app/Models/SsoAuthFailure.php` |
| The monthly usage reconciliation dates its period with naive strings and no stated zone. | `M98` | 10318 | `app/Jobs/Entitlements/ReconcileTenantUsageJob.php` · `config/database.php` · `tests/Feature/Tenancy/ConnectionTopologyTest.php` · `config/app.php` +2 |
| No runbook rotates `APP_KEY`; rotation is only named as a manual step. | `M93` | 9585 | *hub files only* |
| `throttle:saml-acs`'s route BINDING is asserted by nothing, while its registration is. | `M30` | 3364 | `tests/Feature/Auth/RateLimiterBindingTest.php` |
| The framework's own mail components interpolate into attributes the same way, and the M57 gate cannot reach them. | `M57` | 4676 | `scripts/mail-attribute-lint.php` |
| `deploy.yml`'s `workflow_run` filter matches a fork pull request whose head branch is named `main`. | `M98` | 10263 | `.github/workflows/deploy.yml` |
| No standing gate proves compatibility with the testing server's PostgreSQL 15. | `M95` | 9799 | `app/Support/Analytics/AnalyticsQuery.php` · `app/Services/Analytics/AnalyticsMetricsService.php` |
| The per-endpoint `include_answers: true` webhook payload opt-in has no key anywhere — and four other files appear to record it as a deferral already taken. | `M80` | 7671 | `docs/data-privacy-gdpr-compliance.md` · `docs/piping-output-encoding-design.md` · `config/webhooks.php` |
| Twelve existing tenant-context call sites restore in a `finally` INSIDE their transaction, which is the shape `TenantContext::runFor()` was deliberately built … | `M3` | 734 | — *no file harvested* |
| M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t… | `M5` | 836 | — *no file harvested* |
| A 5xx that arrives AFTER the provider committed is still re-driven. | `M5` | 849 | — *no file harvested* |
| A rotated token can still be lost in the one-UPDATE window M6 left. | `M6` | 901 | — *no file harvested* |
| `users.last_active_tenant_id` has no writer anywhere in `app/`. | `M8` | 2606 | — *no file harvested* |
| `gamification:backfill --sync` reports failure after it has already committed every award. | `M32` | 4047 | — *no file harvested* |
| The database's uniqueness domain and the runtime's resolution domain now disagree about case, and `FormSlug::isTaken()` is the third resolver. | `M61` | 4942 | — *no file harvested* |
| `SubmissionFinalizer` is a SECOND writer of `submission_answers.form_version_id`, and it would erase the evidence of a divergence rather than record it. | `M73` | 6503 | — *no file harvested* |
| Search and the sidebar hide plan-gated destinations when no plan catalog is seeded, while the routes serve them. | `M94` | 9645 | — *no file harvested* |
| In-app notifications are never pruned, and "Mark all as read" is one unbounded update. | `M94` | 9680 | — *no file harvested* |
| A background job that waits in the queue longer than six hours is failed without ever running. | `M94` | 9685 | — *no file harvested* |
| Five screen-reader-only live regions have no positioned ancestor inside their component. | `M94` | 9717 | — *no file harvested* |
| The refresh job's lock is released before its write commits. | `M95` | 9827 | — *no file harvested* |
| The seeders' zero-row guard is blind to a role that does not bypass RLS. | `M95` | 9838 | — *no file harvested* |
| stancl resolves a multi-level host differently from `PlatformHost`. | `M95` | 9847 | — *no file harvested* |
| Airtable computed fields can be mapped as columns a rule writes. | `M96` | 9907 | — *no file harvested* |
| A worker relaunched by NSSM inside the deploy window starts on a half-swapped checkout. | `M96` | 9939 | — *no file harvested* |
| The Sheets adapter's first-tab fallback is `Sheet1`, and the directory's is the first tab. | `M96` | 9947 | — *no file harvested* |
| Offset-paginated logs order by a second-precision timestamp with no unique tie-breaker. | `M96` | 9954 | — *no file harvested* |
| API v1 must send a mapping fingerprint in an undocumented format, and a blank heading is refused there. | `M96` | 9971 | — *no file harvested* |
| `MdsDataTable`'s client-side sort has no mounted test. | `M96` | 9992 | — *no file harvested* |
| bcrypt reads only a password's first 72 bytes. | `M96` | 10012 | — *no file harvested* |
| A previous release's `vendor` that cannot be deleted stops every later deploy before its window. | `M96` | 10049 | — *no file harvested* |
| Whether the testing network hairpins was never measured, and a runbook instruction is unsafe until it is. | `M99` | 10493 | — *no file harvested* |
| The workspaces page prescribed for the central-host loop cannot be built: `pgsql_auth` has no grant on `tenants` or `domains`. | `M99` | 10513 | — *no file harvested* |

## Not live — listed, not hidden

These are stated limits, deliberate decisions, blind spots and deployment obligations
rather than defects in this tree. `scripts/loop.php` already refuses them, and they are
listed here rather than dropped so that this file and that stop rule cannot disagree.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| The data dictionary names nine of the forty-five constraints the migrations declare, while enumerating constraints exhaustively in places. | `M46` | 5051 | *hub files only* |
| The citation-liveness gate cannot see a behaviour negative, and its ledger ceiling counts deliberately-preserved historical filings. | `M46` | 5059 | *hub files only* |
| `routes/tenant.php`'s ~95 `can:` gates get none of M63's checks, and the derived half is expected to FIND something. | `M63` | 3706 | *hub files only* |
| `docs/backlog-triage.md` keeps a tier-1 citation exemption whose stated reason stopped being true in the same increment. | `M65` | 3808 | *hub files only* |
| Any tenant-scoped policy read mounted on the Fortify group is silently blind, and two increments have now walked into it. | `M68` | 2692 | *hub files only* |
| Four P3a refusal cases assert the exception CLASS and never the message. | `M12` | 2006 | `tests/Feature/Submissions/SubmissionDraftServiceTest.php` |
| The liveness marker is gated for presence and nothing checks that a verdict is CORRECT — and the error rate of judging one is now measured rather than assumed. | `M65` | 3819 | `tests/Feature/Docs/BacklogProvenanceTest.php` |
| Making the repository private breaks the server's fetch, starts billing the CI minutes and may silently disarm the merge gate. | `M98` | 10244 | `.github/workflows/ci.yml` |
| Four `DO NOT RE-ASK` user decisions of record now live in `PROGRESS_ARCHIVE.md` rather than in `docs/claims/decisions.md`. | `M45` | 5416 | *hub files only* |
| The command gate reads `README.md` only, and three other documents carry runnable command blocks. | `M59` | 4900 | *hub files only* |
| `gamification:backfill --dry-run` cannot reveal a mis-scoring defect, by construction. | `M24` | 2107 | `app/Services/Gamification/BackfillTally.php` |
| Every `MaintenanceJob` fan-out is proved one file at a time, so a future one inherits no coverage. | `M44` | 4008 | `tests/Feature/Queue/JobContractTest.php` |
| Thirteen Vitest stubs across four files are silently inert. | `M23` | 2998 | `resources/js/Pages/submissions/show.test.ts` · `resources/js/components/sso/cards.test.ts` · `resources/js/components/sso/SsoPolicyCard.test.ts` · `resources/js/Layouts/AppLayout.test.ts` |
| Nothing checks that a `§D<n>` citation names a section whose text supports it. | `M7` | 5225 | — *no file harvested* |
| The sync surface's read and write are gated on different permission families, so no single non-admin role can complete the offline loop. | `M13` | 1927 | — *no file harvested* |
| A semantic token is no guarantee of a visible element, and one more instance is probably out there. | `M23` | 3011 | — *no file harvested* |
| the dashboard card ranks the whole tenant to compute three numbers that need no ranking. | `M26` | 2132 | — *no file harvested* |
| Two SSO test files justify a real assertion with a rationale that is false on this framework version. | `M30` | 3374 | — *no file harvested* |

## Hub files — a batch may touch at most one

Derived, not listed: a file cited by **3 or more** open rows. These are
mostly meta-files rather than product code, which is why the rows that share them are not
really coupled — and why excluding them is what makes the queue above separable.

| File | Open rows citing it |
|---|---|
| `docs/deployment-infrastructure.md` | 19 |
| `docs/security-threat-model.md` | 15 |
| `docs/data-dictionary.md` | 13 |
| `routes/tenant.php` | 13 |
| `scripts/backlog-triage.php` | 11 |
| `docs/pipeline.md` | 9 |
| `routes/api.php` | 9 |
| `docs/feature-backlog.md` | 8 |
| `scripts/state.php` | 8 |
| `scripts/citation-liveness-lint.php` | 7 |
| `docs/backlog-triage.md` | 7 |
| `docs/api-specification.md` | 7 |
| `bootstrap/app.php` | 7 |
| `docs/claims/decisions.md` | 6 |
| `scripts/mutate.php` | 5 |
| `scripts/pipeline.php` | 5 |
| `docs/adr/0007-async-execution-substrate.md` | 5 |
| `scripts/next.php` | 5 |
| `docs/architecture/technical-architecture.md` | 5 |
| `scripts/loop.php` | 4 |
| `docs/PRD.md` | 4 |
| `scripts/test-pointer-lint.php` | 4 |
| `docs/gate-baselines.md` | 4 |
| `docs/non-functional-requirements.md` | 4 |
| `config/fortify.php` | 4 |
| `docs/claims/lane-a.md` | 4 |
| `docs/webhook-integration-design.md` | 4 |
| `docs/ACCESS-MATRIX.md` | 3 |
| `scripts/tracker-lint.php` | 3 |
| `scripts/gate-baselines.php` | 3 |
| `docs/ux/design-system-reference.md` | 3 |
| `app/Providers/AppServiceProvider.php` | 3 |
| `app/Models/Form.php` | 3 |
| `database/migrations/2026_07_06_000201_create_forms_table.php` | 3 |
| `routes/console.php` | 3 |
| `resources/views/mail/notification.blade.php` | 3 |
| `docs/TESTING-GUIDE.md` | 3 |

## What to take next

Not here. **`docs/pipeline.md` § Next** names the work, ordered by tier and then by readiness — one
picker, not two. This file keeps its own job: the operability ranking inside the defect ledger, and
the hub set above, which `D13` still uses to group the rows of one tier into an increment.

## What this file cannot see

**82 open row(s) cite no file this script could resolve**, and **57 carry at least one citation that does not resolve.** They are ranked below rows whose citations
all resolve, because a row must be re-derived before it is worked rather than merely worked —
but the ranking is the only consequence: an unresolvable citation is not evidence the row is
wrong. **Batch these by hand**, since a collision check needs files to compare.

Rows with no harvested file:

- `docs/feature-backlog.md:856` — `SlackConnector::deliver()` has the same non-idempotent shape and is deliberately not covered.
- `docs/feature-backlog.md:2425` — The tenant-facing SSO domains card on `/settings/sso` does not exist, so verification is operator-assisted.
- `docs/feature-backlog.md:2440` — `MemberController::invite()` validates `['required', 'email', 'max:255']` and a role, with no domain-ownership check.
- `docs/feature-backlog.md:2451` — Self-registration remains a way to occupy an address in a domain you do not control.
- `docs/feature-backlog.md:630` — `MdsSegmentedControl` spills 30px INSIDE the builder's config pane — a real horizontal scrollbar, and not the page-overflow defect it was filed as.
- `docs/feature-backlog.md:1407` — The storage-quota line counts strangers' submissions.
- `docs/feature-backlog.md:4894` — Three gate invocations fetch `http-server` from the network at run time, and nothing declares it.
- `docs/feature-backlog.md:1990` — The promote route still documents neither of its two 403 causes nor its 422, and the generator cannot narrow a refusal family to one route.
- `docs/feature-backlog.md:1835` — The per-item `error.code` is the integrator's real branching key on `/sync/submissions`, and the contract still publishes it as an unconstrained string.
- `docs/feature-backlog.md:6134` — `M59`'s undeclared-`http-server` row undercounts on both of its numbers.
- `docs/feature-backlog.md:6342` — `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index …
- `docs/feature-backlog.md:6376` — The `npm audit` judge makes a REQUIRED context green while nothing was measured, and that is a deliberate trade rather than an oversight.
- `docs/feature-backlog.md:6607` — What a media cell should SAY beyond a filename, and what shape a grid answer takes in a spreadsheet, are two product questions `M74` deliberately did not answe…
- `docs/feature-backlog.md:6824` — The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s…
- `docs/feature-backlog.md:6883` — A correction still cannot be autosaved, and the reason is an endpoint that does not exist plus a product decision nobody has taken.
- `docs/feature-backlog.md:7315` — `forms.timezone` is documented as defaulting to NULL and the database defaults it to `'UTC'`.
- `docs/feature-backlog.md:7322` — Two columns are documented under the wrong table in the data dictionary.
- `docs/feature-backlog.md:7329` — Nine live tables have no dictionary section, and they are not all framework scaffolding.
- `docs/feature-backlog.md:5981` — A pipeline marker placed mid-document SILENTLY INVALIDATES every `path:N` citation beneath it, and only the ones that land on a blank line are caught.
- `docs/feature-backlog.md:8098` — Six `Default` cells describe a column as application-supplied when the database does supply a default, and one of the two words used is factually wrong.
- `docs/feature-backlog.md:8110` — Three `tenants` cells disagree with the live schema on Type and Nullable, and no gate compares either column.
- `docs/feature-backlog.md:8347` — `promote()`'s pre-lock shape has two more instances, and one of them is 74 lines above the row that named it.
- `docs/feature-backlog.md:8418` — Four citations point into UNTRACKED trees and can never resolve, and nothing separates them from citations that are merely broken.
- `docs/feature-backlog.md:8699` — `.sheets-fields` is probably the worst `MdsSegmentedControl` host in the tree and it is the one with no coverage of any kind.
- `docs/feature-backlog.md:8837` — Three more lock-holding transitions DELETE the child rows a concurrent builder edit is writing, and for all three the RLS backstop does not fire.
- `docs/feature-backlog.md:8847` — `FormService` has seven lock-free writers to `forms` and one locked, and six of them build their audit payload from a model nobody re-read.
- `docs/feature-backlog.md:8858` — One `forceFill` writes `closes_at` and `max_responses` together, and the acceptance guard treats one as authoritative-under-lock and the other as ignorable-pre…
- `docs/feature-backlog.md:9326` — Standing Rule 7(b-bis) says "Three files" against a table of five rows, inside the rule that exists to keep paired files in step.
- `docs/feature-backlog.md:9462` — The mirror gate's comment stripper is line-based and unscoped to `<script>`, so its property grammar on a `.vue` file is first-match-wins across the template a…
- `docs/feature-backlog.md:9651` — In the builder at compact widths, selecting a field does not bring its settings on screen.
- `docs/feature-backlog.md:9657` — The builder's side-pane widths are fixed pixels, so at the largest text size the pane-switch threshold no longer matches the columns it is derived from.
- `docs/feature-backlog.md:9662` — A later successful builder save clears the unsaved warning for an earlier field that still failed.
- `docs/feature-backlog.md:9668` — A repeated identical builder save failure is announced to a screen reader only once.
- `docs/feature-backlog.md:9672` — The builder toolbar says "All changes saved" on a form that cannot be edited.
- `docs/feature-backlog.md:9676` — The builder's "Not saved" status looks the same as "All changes saved".
- `docs/feature-backlog.md:9691` — Discarding a conflicted offline response uses the browser's plain confirm box.
- `docs/feature-backlog.md:9696` — In an installed offline form, opening another form's queued response leaves the installed window.
- `docs/feature-backlog.md:9701` — Reviewing an offline conflict reopens a form that has since closed or filled up, and the resubmit is then refused.
- `docs/feature-backlog.md:9705` — Two accessibility specs still scan only the open dialog rather than the whole page.
- `docs/feature-backlog.md:9724` — The password-strength requirement list loses its list semantics in Safari, and 21 more files carry the same shape.
- `docs/feature-backlog.md:9783` — The token refresh re-check cannot stop a redundant rotation.
- `docs/feature-backlog.md:9792` — A connector test send has no token pre-flight.
- `docs/feature-backlog.md:9809` — Operator commands have no well-formed audit shape.
- `docs/feature-backlog.md:9869` — The queue enum's docblock says five names.
- `docs/feature-backlog.md:9931` — A deploy deletes the previous build's chunks, so an open tab can fail to load a script it needs.
- `docs/feature-backlog.md:9963` — The inbox orders a promoted draft by when it was started, not when it was submitted.
- `docs/feature-backlog.md:9999` — Seven list pages hand-roll the same one-line hint above a table.
- `docs/feature-backlog.md:10005` — The `email` rule accepts a local part longer than 64 characters.
- `docs/feature-backlog.md:10024` — Two delivery logs page with no loading state.
- `docs/feature-backlog.md:10029` — Pressing Check on a Sheets rule whose tab is not the first re-reads the first tab and drops the rule's bindings.
- `docs/feature-backlog.md:10503` — The `md-status` check the operator is told to read returns a Laravel page, because the front-controller rewrite swallows it.
- `docs/feature-backlog.md:10549` — The member roster 500s when a membership names a user the query cannot return.
- `docs/feature-backlog.md:734` — Twelve existing tenant-context call sites restore in a `finally` INSIDE their transaction, which is the shape `TenantContext::runFor()` was deliberately built …
- `docs/feature-backlog.md:836` — M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t…
- `docs/feature-backlog.md:849` — A 5xx that arrives AFTER the provider committed is still re-driven.
- `docs/feature-backlog.md:901` — A rotated token can still be lost in the one-UPDATE window M6 left.
- `docs/feature-backlog.md:2606` — `users.last_active_tenant_id` has no writer anywhere in `app/`.
- `docs/feature-backlog.md:4047` — `gamification:backfill --sync` reports failure after it has already committed every award.
- `docs/feature-backlog.md:4942` — The database's uniqueness domain and the runtime's resolution domain now disagree about case, and `FormSlug::isTaken()` is the third resolver.
- `docs/feature-backlog.md:6503` — `SubmissionFinalizer` is a SECOND writer of `submission_answers.form_version_id`, and it would erase the evidence of a divergence rather than record it.
- `docs/feature-backlog.md:9645` — Search and the sidebar hide plan-gated destinations when no plan catalog is seeded, while the routes serve them.
- `docs/feature-backlog.md:9680` — In-app notifications are never pruned, and "Mark all as read" is one unbounded update.
- `docs/feature-backlog.md:9685` — A background job that waits in the queue longer than six hours is failed without ever running.
- `docs/feature-backlog.md:9717` — Five screen-reader-only live regions have no positioned ancestor inside their component.
- `docs/feature-backlog.md:9827` — The refresh job's lock is released before its write commits.
- `docs/feature-backlog.md:9838` — The seeders' zero-row guard is blind to a role that does not bypass RLS.
- `docs/feature-backlog.md:9847` — stancl resolves a multi-level host differently from `PlatformHost`.
- `docs/feature-backlog.md:9907` — Airtable computed fields can be mapped as columns a rule writes.
- `docs/feature-backlog.md:9939` — A worker relaunched by NSSM inside the deploy window starts on a half-swapped checkout.
- `docs/feature-backlog.md:9947` — The Sheets adapter's first-tab fallback is `Sheet1`, and the directory's is the first tab.
- `docs/feature-backlog.md:9954` — Offset-paginated logs order by a second-precision timestamp with no unique tie-breaker.
- `docs/feature-backlog.md:9971` — API v1 must send a mapping fingerprint in an undocumented format, and a blank heading is refused there.
- `docs/feature-backlog.md:9992` — `MdsDataTable`'s client-side sort has no mounted test.
- `docs/feature-backlog.md:10012` — bcrypt reads only a password's first 72 bytes.
- `docs/feature-backlog.md:10049` — A previous release's `vendor` that cannot be deleted stops every later deploy before its window.
- `docs/feature-backlog.md:10493` — Whether the testing network hairpins was never measured, and a runbook instruction is unsafe until it is.
- `docs/feature-backlog.md:10513` — The workspaces page prescribed for the central-host loop cannot be built: `pgsql_auth` has no grant on `tenants` or `domains`.
- `docs/feature-backlog.md:5225` — Nothing checks that a `§D<n>` citation names a section whose text supports it.
- `docs/feature-backlog.md:1927` — The sync surface's read and write are gated on different permission families, so no single non-admin role can complete the offline loop.
- `docs/feature-backlog.md:3011` — A semantic token is no guarantee of a visible element, and one more instance is probably out there.
- `docs/feature-backlog.md:2132` — the dashboard card ranks the whole tenant to compute three numbers that need no ranking.
- `docs/feature-backlog.md:3374` — Two SSO test files justify a real assertion with a rationale that is false on this framework version.
