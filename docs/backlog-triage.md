# Backlog triage

**Generated by `scripts/backlog-triage.php`. Do not hand-edit — regenerate it.**

**Measured 2026-09-24** against `origin/main` at `4a53c3fd1bbf2f3ddff6c8eb7e5c283445095abb` · 245 open rows · 0 `major` · 241 `minor` · 4 `nit`

⚠️ **This is an OPERABILITY order, not a priority order, and the difference is why the file it
replaced went wrong.** It cannot tell you what matters most. Severity would, and there are zero
open `major` rows, so that field separates nothing. What it derives is which rows are still
**live**, whose **citations still resolve**, and which rows **share no files** with each other —
which is what picking a batch needs, and is all this file claims.

⛔ **Liveness here is a marker a human wrote, not a measurement this script made.** It is gated
for presence by `tests/Feature/Docs/BacklogProvenanceTest.php` and reported by
`scripts/state.php`; neither checks that a verdict is *right*. A row marked live that is dead
passes everything. The mutation harness is what settles a row, and settling it is the job of
whichever increment takes it.

📌 **The 2026-08-28 census this file replaced is preserved byte-for-byte in `docs/backlog-triage-m37.md`.**
Its findings about the sweep's own accuracy, and the brief it ran under, are not reproduced here
and are not stale — they are a dated record and are still worth reading before any fan-out.

---

## Census

| | Rows |
|---|---|
| Open | **245** |
| — still `live` | **167** |
| — `latent`, needs a stated precondition | 60 |
| — `not live`, not a defect in this tree | 18 |
| Severity bullets, ever | 481 |

## The queue — live rows, most operable first

Ordered by citation health, then by how few non-hub files the row touches, then oldest
debt first. `cites` lists only the files a batch would collide on; hub files are excluded
from it and listed separately below.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| A claim file has no constrained form for a forward declaration, so the one stale declaration on the tree cannot be gated. | `M42` | 4240 | *hub files only* |
| `scripts/mutate.php` cannot drive a positive control for anything that is not Pest in a container. | `M42` | 4367 | *hub files only* |
| A tracker surgery whose diff touches only `paths-ignore`d files produces no post-merge run at all, so `R7`'s trunk arm is unreachable for exactly the diff shap… | `M48` | 5498 | *hub files only* |
| `submission_geo_index` is a real table and the data dictionary does not mention it at all. | `M58` | 4775 | *hub files only* |
| `docker compose run --rm e2e` does not work on this host as three documents prescribe it, and the most likely wrong form EXITS 0. | `M61` | 4996 | *hub files only* |
| `R7`'s byte threshold was calibrated on surgeries that were all left too late, so acting EARLY lands in its dead zone. | `M71` | 5983 | *hub files only* |
| `docs/backlog-triage.md` is generated stale by its own close-out, and the drift is on the trunk now. | `M71` | 6087 | *hub files only* |
| `scripts/next.php` never clips the release HEADING, so a third of the hand-off is byte-identical noise. | `M72` | 6359 | *hub files only* |
| The `npm audit` judge makes a REQUIRED context green while nothing was measured, and that is a deliberate trade rather than an oversight. | `M72` | 6376 | *hub files only* |
| What actually delivers the offline mis-cased render is unknown, and TWO confident models of it have now been wrong. | `M73` | 6390 | *hub files only* |
| The triage generator silently drops any citation written as a PARTIAL path, and a partial path is strictly worse than a bare filename. | `M73` | 6462 | *hub files only* |
| `scripts/loop.php --assess` does not read `docs/pipeline.md`, so it cannot refuse a row on its PIPELINE state. | `M79` | 5979 | *hub files only* |
| The `pipeline-lint` controls mirror the LIVE corpus, so a defect in one of the four coverage predicates reddens all 38 cases and drowns the control that names … | `M82` | 7937 | *hub files only* |
| Every close-out drifts `docs/pipeline.md`, because a status bullet added to `PROGRESS.md` shifts the two markers at its end and the generated line cites them b… | `M82` | 7993 | *hub files only* |
| `pipeline.php --check` compares the file only from `## The line` onward, so the BANNER — the one part a human would hand-correct, and the part `state.php` read… | `M82` | 8021 | *hub files only* |
| The citation gate asserts a cited line is ALIVE, never that it still says what the citation claims — so re-pointing a shifted citation arithmetically merges gr… | `M83` | 8055 | *hub files only* |
| Two PRD acceptance criteria are dispositioned in INDENTED CONTINUATION bullets the collector skips, so `P2e`'s residue reconciles against its own predicate rat… | `M84` | 8338 | *hub files only* |
| `docs/ACCESS-MATRIX.md` cites a HOSTNAME AND PORT, which the citation pattern parses as a path and line. | `M85` | 8436 | *hub files only* |
| `LEDGER_ROT_CEILING` cannot ratchet down while preserved original filings are counted, and `M85` has now paid that in the other direction. | `M85` | 8456 | *hub files only* |
| The generated queue's ORDER depends on which UNTRACKED directories happen to exist, and `docs/pipeline.md` is merge-gated on that order. | `M85` | 8467 | *hub files only* |
| `scripts/tracker-lint.php` is cited by ZERO of the open rows while owning the defects of at least two of them, so no collision check can ever see it. | `M86` | 8516 | *hub files only* |
| The archived status bullets live in TWO non-contiguous places, and that is what made the batch numbering go wrong. | `M86` | 8566 | *hub files only* |
| Eleven value-domain `CHECK` constraints guard columns the enum catalog has no row for at all, and the fix cannot be made while the citation ledger is at its ce… | `M87` | 8654 | *hub files only* |
| `FormService` has seven lock-free writers to `forms` and one locked, and six of them build their audit payload from a model nobody re-read. | `M88` | 8853 | *hub files only* |
| One `forceFill` writes `closes_at` and `max_responses` together, and the acceptance guard treats one as authoritative-under-lock and the other as ignorable-pre… | `M88` | 8864 | *hub files only* |
| `P2d`'s literal-`null` blind spot hides three genuinely inert columns, and the honest repair needs the plumbing its own row says it does not. | `M89` | 9049 | *hub files only* |
| `D13`'s one-hub-row cap cannot admit a new lint gate at all, and `M90` broke it for exactly that reason. | `M90` | 9238 | *hub files only* |
| Standing Rule 7(b-bis) says "Three files" against a table of five rows, inside the rule that exists to keep paired files in step. | `M91` | 9332 | *hub files only* |
| A third instance of the citation class `M83` filed in the abstract: three live lines that say nothing about what cites them. | `M91` | 9358 | *hub files only* |
| `scripts/` is the one tree `test-pointer-lint` still cannot scan, and it cannot become a root while the gate's own header is written the way it is. | `M92` | 9412 | *hub files only* |
| A `FormRequest` class docblock is PUBLISHED API DOCUMENTATION, and nothing says so at the place where someone writes one. | `M92` | 9503 | *hub files only* |
| A gate's SUMMARY LINE is a paired artefact across three files, and nothing declares that pairing at the place where somebody edits one. | `M92` | 9519 | *hub files only* |
| A struck closure records no closer. | `M95` | 9861 | *hub files only* |
| ADR-0007 points at rows of ADR-0005 by bare line number, and every pointer lands on the wrong row. | `M95` | 9887 | *hub files only* |
| The Next section and the hand-off list open decisions as work to take. | `M96` | 9921 | *hub files only* |
| ADR-0007 points at the runbook by bare line numbers that have moved. | `M96` | 9992 | *hub files only* |
| The design-system reference describes filter chips, a clear-all action and width-matched skeleton cells that no list renders. | `M96` | 10024 | *hub files only* |
| The invitation's sending domain publishes no DMARC and no aligned DKIM, and an emailed invitation is the only way a tester gets an account. | `M98` | 10177 | *hub files only* |
| The ledger's header sends new work to a section no row has been filed under since `M93`. | `M98` | 10311 | *hub files only* |
| The first-boot runbook and the checklist set up none of the database backup §5 prescribes. | `M98` | 10418 | *hub files only* |
| PROGRESS.md is within about five close-outs of the tracker-lint byte ceiling. | `M98` | 10476 | *hub files only* |
| Three of this ledger's own line citations were wrong before `M100` started, and an unrelated six-line insertion silently corrected two of them. | `M100` | 10649 | *hub files only* |
| This repository asserts what the off-repo Testing Server Checklist says, in prose no gate can check, and one such assertion was false for at least one incremen… | `M101` | 10718 | *hub files only* |
| A claim file's line citations are wrong the moment they are pushed, and three of them in one paragraph show the mechanism rather than a mistake. | `M102` | 10731 | *hub files only* |
| `.github/workflows/ci.yml:84` asserts in the present tense that `DEPLOY_ENABLED` is unset, which `D47` made false three days before `D53` was decided on the op… | `M105` | 10868 | *hub files only* |
| The resume READ escapes service-worker caching only because its path prefix is `drafts/` rather than `f/`, so one route rename re-opens the exposure `D20` was … | `M105` | 10920 | *hub files only* |
| `.kilo/worktrees/obsidian-dogwood` is a full second checkout of this repository that is untracked and absent from `.gitignore`, so every host-side sweep double… | `M105` | 10940 | *hub files only* |
| `PROGRESS.md`'s `M101` bullet records a state of the tree that does not exist. | `M106` | 11079 | *hub files only* |
| Nothing relates a decision that RE-TIERS a row to the row it re-tiers, and `P7d` deliberately does not close that half. | `M108` | 11104 | *hub files only* |
| A retier's PAYLOAD is a change to another file's row, nothing performs it, and the only record of that is a lane ledger. | `M108` | 11118 | *hub files only* |
| `scripts/backlog-triage.php --check` works, is the only drift detector for a generated file in `paths-ignore`, and is invoked by nothing. | `M108` | 11144 | *hub files only* |
| `docs/pipeline.md` is cited BY LINE in at least two places, it is regenerated every increment, and the citation gate cannot see the rot. | `M108` | 11155 | *hub files only* |
| `SlackConnector::deliver()` has the same non-idempotent shape and is deliberately not covered. | `M5` | 856 | `app/Support/Connectors/Providers/SlackConnector.php` |
| The tenant-facing SSO domains card on `/settings/sso` does not exist, so verification is operator-assisted. | `M18` | 2425 | `app/Services/Sso/SsoConnectionPresenter.php` |
| `MemberController::invite()` validates `['required', 'email', 'max:255']` and a role, with no domain-ownership check. | `M18` | 2440 | `app/Http/Controllers/Tenant/MemberController.php` |
| Self-registration remains a way to occupy an address in a domain you do not control. | `M18` | 2451 | `app/Services/Settings/RegistrationGate.php` |
| `tracker-lint` R8 guards `CLAUDE.md` and cannot reach `PROGRESS.md`, which is the half that actually rotted. | `M42` | 4252 | `scripts/tracker-lint-controls.php` |
| `scripts/next.php` takes each release's LEAD paragraph, and a lead paragraph is often a file manifest rather than the lesson. | `M42` | 4387 | `docs/claims/TEMPLATE.md` |
| `docs/data-dictionary.md` documents nine `tenants` columns that exist in no migration, and its enum catalog contradicts the enum. | `M72` | 6308 | `app/Enums/TenantStatus.php` |
| `public-runtime-offline.spec.ts`'s parked-conflict case is FLAKY across viewport projects, and `D2` makes that a merge-blocking property. | `M77` | 7183 | `resources/public-runtime/sw.ts` |
| The standing `pgsql_auth` rule was never literally true, and its stated revisit trigger names a different grant. | `M78` | 7410 | `app/Services/Auth/GoogleSignInProvisioner.php` |
| A pipeline marker placed mid-document SILENTLY INVALIDATES every `path:N` citation beneath it, and only the ones that land on a blank line are caught. | `M79` | 5981 | `docs/adr/0008-entitlement-and-metering.md` |
| §7.1 names a `/api/v1/webhooks/endpoints` path segment that exists nowhere in the tree, and it is a floor rather than a census. | `M80` | 7600 | `tests/Feature/Webhooks/WebhookEndpointApiTest.php` |
| "1 concurrent sync export per form, additional requests 429" — no concurrency guard exists on any export path. | `M80` | 7802 | `app/Jobs/MaintenanceJob.php` |
| `scripts/citation-liveness-lint.php` sits AT its ceiling with zero headroom, so any edit that shifts a line in `docs/data-dictionary.md` is a merge failure and… | `M83` | 8039 | `docs/adr/0008-entitlement-and-metering.md` |
| The triage generator harvested a row as hub-free whose repair could only be made in a hub file, and the batch it then proposed violated `D13`. | `M83` | 8175 | `tests/Feature/Migrations/DocumentedDefaultDriftTest.php` |
| `scripts/mutate.php` cannot prove ANY gate whose truthful container answer is RED, and a shipped gate is already in that state. | `M84` | 8273 | `tests/Feature/Docs/SuiteCollectionFloorTest.php` |
| No control harness drives the real `scripts/backlog-triage.php` or `scripts/pipeline.php`, and the one harness that looks like it does stubs the generator. | `M86` | 8579 | `tests/Feature/Docs/PipelineLintControlsTest.php` |
| A `docs/feature-backlog.md` citation into the exceptions log resolves to a live line about a different subject, and the citation gate is built to pass exactly … | `M87` | 8717 | `resources/js/composables/useServerAutosave.ts` |
| The optimistic-concurrency token §8 substituted for the draft lock is CLIENT-OPTIONAL, and a shipped test pins it that way. | `M88` | 8833 | `docs/form-versioning-schema-migration.md` |
| Three client-side workarounds were built ON the JSON-refusal defect `M90` fixed, and all three can now be simplified — but one of them is load-bearing until it… | `M90` | 9226 | `docs/gamification-design.md` |
| `MIRROR_DIVERGENCES` is keyed by mirror NAME alone, so two same-named mirrors in different files would silently share one exception. | `M92` | 9424 | `tests/Feature/Docs/DocumentedEnumMirrorDriftTest.php` |
| `docs/data-dictionary.md` holds a fourth copy of `bot_challenge` in prose and `FormBotChallenge` is in no catalog row, and `M92` could not take it. | `M92` | 9493 | `app/Enums/FormBotChallenge.php` |
| Four super-admin console pages have no axe scan: `tests/e2e/admin-console-axe.spec.ts` covers only the settings and feedback pages. | `M93` | 9603 | `tests/e2e/admin-console-axe.spec.ts` |
| The builder's side-pane widths are fixed pixels, so at the largest text size the pane-switch threshold no longer matches the columns it is derived from. | `M94` | 9663 | `resources/js/Pages/forms/Builder.vue` |
| A later successful builder save clears the unsaved warning for an earlier field that still failed. | `M94` | 9668 | `resources/js/components/builder/save-state.test.ts` |
| The token refresh re-check cannot stop a redundant rotation. | `M95` | 9789 | `app/Services/Connectors/ConnectionTokenRefresher.php` |
| Operator commands have no well-formed audit shape. | `M95` | 9815 | `app/Support/Audit/AuditLogger.php` |
| The queue enum's docblock says five names. | `M95` | 9875 | `app/Enums/QueueName.php` |
| PHP's file-size limits in the dev image reject attachments the app allows. | `M95` | 9879 | `config/attachments.php` |
| The inbox orders a promoted draft by when it was started, not when it was submitted. | `M96` | 9969 | `app/Services/Submissions/SubmissionInboxPresenter.php` |
| `TopNav.vue` says the tenant switcher does not exist yet, and nothing queues it. | `M96` | 9986 | `resources/js/components/shell/TopNav.vue` |
| Five of seven paths on the testing site carry none of the four app security headers, and nothing anywhere says whether that is intended. | `M101` | 10681 | `app/Http/Middleware/AppSecurityHeaders.php` |
| Everything this repository knows about the testing box's Apache configuration is prose no gate can read, and `M102` added four more sentences of it. | `M102` | 10747 | `scripts/staging-headers-judge.php` |
| The testing site's port-80 vhost is outside the crawl-protection gate's reach entirely, and this repository recorded it as having no listener at all. | `M102` | 10763 | `scripts/staging-headers-judge.php` |
| `docs/ACCESS-MATRIX.md:105` tells its reader the central host answers a workspace route with a 404, and it does not — it redirects. | `M105` | 10857 | `tests/Feature/Tenancy/CentralHostFallbackTest.php` |
| A bare Vue component name is invisible to the citation harvester, and PSR-4 cannot reach it. | `M108` | 11131 | `app/Http/Controllers/Tenant/MemberController.php` |
| `CLAUDE.md`'s gate table sends PHPStan to the container, one row below the rule that explains why the container is wrong. | `M76` | 6899 | `database/migrations/2026_07_06_000205_create_form_fields_table.php` · `app/Models/FormField.php` |
| `forms.allow_manual_encoding` is documented as Feature #7's capability flag and has neither a reader nor a writer — the only one of five inert `allow_*` flags … | `M80` | 7450 | `tests/Feature/Tenancy/TenantExtractColumnDriftTest.php` · `app/Policies/SubmissionPolicy.php` |
| §7.1's `Form draft` row pins four `/api/v1` builder endpoints registered nowhere, and the `validations` sub-resource exists on neither surface. | `M80` | 7530 | `app/Http/Controllers/Tenant/FormBuilderController.php` · `docs/xlsform-interop-spec.md` |
| The API rate-limit table promises 300 requests/minute per authenticated user and no such limiter is defined. | `M80` | 7732 | `app/Providers/FortifyServiceProvider.php` · `app/Providers/QueueServiceProvider.php` |
| `scripts/pipeline-lint.php` P2d recognises a cast by a CLOSED VOCABULARY of cast names, so a cast this project adopts later reads as a write and silently un-do… | `M81` | 7880 | `scripts/pipeline-lint.php` · `app/Services/Attachments/AttachmentStorageService.php` |
| No gate whose input is the GIT INDEX can have a Pest control, because `git` is not installed in the app container. | `M85` | 8409 | `scripts/tracker-lint-controls.php` · `scripts/citation-liveness-lint-controls.php` |
| `.sheets-fields` is probably the worst `MdsSegmentedControl` host in the tree and it is the one with no coverage of any kind. | `M87` | 8705 | `resources/js/components/integrations/SheetsRuleFields.vue` · `tests/e2e/responsive-axe.spec.ts` |
| A `ControlKind` docblock claims it is derived exactly as the SFC's own `control` computed, the two disagree in three ways, and no gate can compare them. | `M92` | 9446 | `resources/public-runtime/lib/types.ts` · `resources/js/components/submissions/FieldInput.vue` |
| In the builder at compact widths, selecting a field does not bring its settings on screen. | `M94` | 9657 | `resources/js/Pages/forms/Builder.vue` · `resources/js/Pages/forms/builder-layout.test.ts` |
| A connector test send has no token pre-flight. | `M95` | 9798 | `app/Http/Controllers/Tenant/ConnectionRuleController.php` · `app/Services/Connectors/ConnectorTester.php` |
| `buildRows`'s docblock argues against the heading match, and one test title gives a reason that no longer holds. | `M96` | 10041 | `resources/js/components/integrations/mapping-model.ts` · `resources/js/components/integrations/mapping-model.test.ts` |
| The first-boot steps leave `LOG_LEVEL=debug` and one unrotated log file in place. | `M98` | 10432 | `.env.example` · `config/logging.php` |
| `public/.htaccess` is tracked, is read by exactly one deployment, and is exercised by no gate in any environment. | `M101` | 10708 | `docker-compose.yml` · `tests/Feature/Deploy/DeployMaintenanceGuardTest.php` |
| The migration-ordering trap is recorded only inside one migration's docblock, where the next author does not look — and `M103` paid for it. | `M103` | 10802 | `database/migrations/2026_07_20_000004_backfill_resource_grants_from_form_collaborators.php` · `scripts/migration-lint.php` |
| `TwoFactorResetService`'s zero-row guard is UNREACHABLE by construction, so no test can see it — `scripts/mutate.php` proved it by surviving. | `M107` | 11021 | `app/Services/Auth/TwoFactorResetService.php` · `tests/Feature/Auth/TwoFactorResetTest.php` |
| The promote route still documents neither of its two 403 causes nor its 422, and the generator cannot narrow a refusal family to one route. | `M67` | 1990 | `app/Exceptions/Submissions/SubmissionValidationException.php` · `app/Support/OpenApi/ModuleDisabledResponseExtension.php` · `app/Support/OpenApi/SubmissionRefusalResponseExtension.php` |
| The per-item `error.code` is the integrator's real branching key on `/sync/submissions`, and the contract still publishes it as an unconstrained string. | `M69` | 1835 | `app/Http/Controllers/Api/V1/SyncSubmissionController.php` · `app/Exceptions/Submissions/FormNotAcceptingSubmissionException.php` · `app/Support/OpenApi/ApiErrorEnvelope.php` |
| Two more scripts have `backlog-triage.php`'s destructive default, and the row that filed that defect names one of them as its harmless counter-example. | `M71` | 6041 | `scripts/regenerate-brand-ramp-fixture.php` · `tests/fixtures/brand-ramp.json` · `scripts/npm-audit-judge.php` |
| What a media cell should SAY beyond a filename, and what shape a grid answer takes in a spreadsheet, are two product questions `M74` deliberately did not answe… | `M74` | 6607 | `app/Policies/AttachmentPolicy.php` · `app/Services/Submissions/SubmissionRowProjector.php` · `app/Services/Connectors/MappableColumnCatalog.php` |
| Three more lock-holding transitions DELETE the child rows a concurrent builder edit is writing, and for all three the RLS backstop does not fire. | `M88` | 8843 | `app/Services/Forms/RestoreService.php` · `app/Services/Xlsform/XlsformImporter.php` · `app/Models/FormVersion.php` |
| The central landing page offers "Create a workspace", and the account it creates has no workspace and is redirected straight back to the central host it just c… | `M93` | 9582 | `resources/js/Pages/Welcome.vue` · `tests/e2e/auth-axe.spec.ts` · `app/Http/Controllers/PlatformLandingController.php` |
| Two accessibility specs still scan only the open dialog rather than the whole page. | `M94` | 9711 | `tests/e2e/analytics-axe.spec.ts` · `tests/e2e/scopes-axe.spec.ts` · `tests/e2e/builder-axe.spec.ts` |
| Every handled tenancy-identification failure is logged at ERROR with a full stack trace. | `M98` | 10341 | `app/Http/Middleware/InitializeTenancyByPublicHost.php` · `app/Models/Domain.php` · `tests/Feature/Tenancy/CentralHostFallbackTest.php` |
| `backlog-triage.php` harvests a fraction of some rows' paths, so `D13`'s overlap grouping is computed from an undercount. | `M99` | 10566 | `resources/views/deploy-window.blade.php` · `public/index.php` · `vendor/autoload.php` |
| The `AppSecurityHeaders` mount census says four and the tree has ten, and the wrong number is written in the code as well as in the threat model. | `M101` | 10670 | `app/Http/Middleware/AppSecurityHeaders.php` · `routes/web.php` · `routes/google-auth.php` |
| The offline outbox's only scope assertion cannot tell device-wide from visit-scoped, and three citations in the files a `D26` repair opens point at moved lines. | `M105` | 10929 | `resources/public-runtime/__tests__/sync-outbox.test.ts` · `packages/design-system/src/theme/__tests__/clipped-node-containment.test.ts` · `resources/public-runtime/components/SyncStatus.vue` |
| The design system has no `key` or `lock` glyph, so two security controls borrow `undo`. | `M107` | 11038 | `packages/design-system/src/components/Icon/icons.ts` · `resources/js/Pages/members/Index.vue` · `resources/js/Pages/admin/Users.vue` |
| Two user-supplied predicates now run on the `pgsql_auth` connection, against a standing rule that says none may. | `M77` | 7025 | `docs/multi-tenancy-rbac-design.md` · `docs/adr/0002-multi-tenancy-shared-db-rls.md` · `app/Actions/Fortify/UpdateUserProfileInformation.php` · `app/Actions/Fortify/CreateNewUser.php` |
| The documented `Users & roles` API resource group (`GET/POST /api/v1/users`, `/api/v1/roles`) has zero routes, and a shipped schema decision was already paid f… | `M80` | 7513 | `app/Http/Resources/Api/V1/AuditResource.php` · `docs/multi-tenancy-rbac-design.md` · `app/Models/Role.php` · `config/permission.php` |
| `scripts/pipeline-lint.php`'s four coverage rules read the MARKDOWN half of the corpus only, and three deferral sentences live in the PHP half where nothing ca… | `M82` | 7896 | `scripts/pipeline-lint.php` · `app/Enums/AnalyticsAxis.php` · `app/Enums/FormBotChallenge.php` · `app/Services/Submissions/SubmissionInboxPresenter.php` |
| `'Unknown user'` is a literal duplicated at six call sites and owned by nothing. | `M103` | 10777 | `app/Services/Audit/AuditLogPresenter.php` · `app/Services/Admin/SuperAdminService.php` · `app/Services/Audit/AuditExporter.php` · `resources/js/components/audit/types.ts` |
| Two tenant cards post every field on every Save, so a stale tab reverts a setting the same way the platform console did. | `M103` | 10788 | `resources/js/components/settings/MaintenanceCard.vue` · `app/Http/Requests/Tenant/UpdateMaintenanceSettingsRequest.php` · `resources/js/components/sso/SsoPolicyCard.vue` · `app/Services/Settings/PlatformSettings.php` |
| The documented async export API — `POST /api/v1/forms/{form}/exports` and `GET /api/v1/exports/{export}` — has zero routes and no `exports` job row, while the … | `M80` | 7493 | `app/Enums/QueueName.php` · `app/Jobs/Submissions/GeneratePdfJob.php` · `app/Enums/AttachmentKind.php` · `app/Enums/NotificationType.php` +1 |
| `promote()`'s pre-lock shape has two more instances, and one of them is 74 lines above the row that named it. | `M85` | 8353 | `app/Enums/FormVersionStatus.php` · `app/Services/Submissions/SubmissionPipeline.php` · `app/Services/Attachments/AttachmentReferenceValidator.php` · `resources/public-runtime/composables/useAutosave.ts` +1 |
| Three comments corrected by `M92` now name an arm that asserts LESS than they claim, and the gap is different in each. | `M92` | 9480 | `app/Services/Forms/FormPresenter.php` · `app/Enums/PointRule.php` · `app/Events/FormCreated.php` · `app/Events/MemberJoined.php` +1 |
| Seventeen line pointers into this ledger resolve to unrelated lines, and no gate reads them. | `M94` | 9736 | `app/Services/Forms/TemplateService.php` · `resources/js/components/builder/ConfigPanel.vue` · `packages/design-system/src/components/Modal/Modal.test.ts` · `tests/e2e/builder-axe.spec.ts` +1 |
| The runbook and checklist steer the testing workspace onto Starter, whose caps bind with no billing. | `M98` | 10397 | `database/seeders/Data/PlanCatalog.php` · `app/Services/Entitlements/QuotaGuard.php` · `app/Services/Entitlements/EntitlementService.php` · `app/Services/Admin/TenantDetailPresenter.php` +1 |
| PHPStan is GREEN in CI and reports 18 errors in the app container on the same commit, so the gate cannot be used locally at all — and this has been true and UN… | `M107` | 11048 | `app/Models/FormField.php` · `app/Services/Forms/BuilderPresenter.php` · `app/Services/Forms/SchemaSnapshotSerializer.php` · `app/Http/Resources/Api/V1/FormVersionResource.php` +1 |
| `docs/api-specification.md:63` states in the present tense that every unsafe request is deduplicated against a 24-hour Redis cache keyed on `(tenant_id, endpoi… | `M80` | 7646 | `app/Services/Submissions/SubmissionPipeline.php` · `app/Services/Webhooks/WebhookEventDispatcher.php` · `app/Services/Gamification/PointsRecorder.php` · `app/Http/Middleware/EnforceGuestFormRateLimit.php` +2 |
| The schedule-acceptance vocabulary has four TypeScript copies and a PHP side that is raw strings rather than an enum, so nothing can ever gate it. | `M92` | 9457 | `resources/public-runtime/lib/types.ts` · `resources/js/Pages/forms/Show.vue` · `resources/js/Pages/submissions/Encode.vue` · `resources/js/Pages/forms/show.test.ts` +2 |
| `forms.single_page_mode` has no write surface outside the seeders, so single-page mode is unreachable for a real tenant — and its documented default disagrees … | `M80` | 7428 | `app/Services/Submissions/EncodeFormPresenter.php` · `app/Services/Submissions/PublicFormPresenter.php` · `database/seeders/E2eSeeder.php` · `tests/Feature/Submissions/EncodeStepPayloadTest.php` +3 |
| The audit spec's §1 table is asserted by nothing, and a static sweep cannot be the thing that asserts it. | `M70` | 5896 | `docs/audit-compliance-logging-spec.md` · `app/Enums/AuditEvent.php` · `app/Services/Tenancy/CustomDomainService.php` · `app/Services/Sso/SsoConnectionService.php` +4 |
| The parity matrix scores API/programmatic import as Phase-1 shipped, and the code's own enum docblock calls it a later channel. | `M80` | 7551 | `docs/competitive-feature-parity-matrix.md` · `app/Enums/SubmissionSource.php` · `app/Http/Controllers/Api/V1/SyncSubmissionController.php` · `app/Http/Controllers/Public/GuestDraftController.php` +4 |
| NFR §8 sets a 30-day soft-delete grace period before a hard-deletion job, and for the three entities it names there is no purge job, no grace-period config val… | `M80` | 7617 | `app/Jobs/Maintenance/PruneFailedJobsJob.php` · `app/Jobs/Submissions/ReapTenantDraftsJob.php` · `app/Services/Submissions/ClientUuidResolver.php` · `app/Policies/FormPolicy.php` +4 |
| `forms.allow_offline_sync` has no reader anywhere — neither the sync manifest nor the PWA install entry honours the per-form offline gate that two documents de… | `M80` | 7472 | `tests/Feature/Tenancy/TenantExtractColumnDriftTest.php` · `docs/erd.md` · `docs/ux/form-filling-ux-flow.md` · `app/Http/Controllers/Api/V1/SyncManifestController.php` +5 |
| Three gate invocations fetch `http-server` from the network at run time, and nothing declares it. | `M59` | 4894 | *hub files only* |
| `scripts/backlog-triage.php --check` is wired into nothing, so the generated triage can drift with no gate saying so. | `M65` | 3795 | *hub files only* |
| `M59`'s undeclared-`http-server` row undercounts on both of its numbers. | `M71` | 6134 | *hub files only* |
| The partial-path citation row's consequences are mostly false, and the measurement that settles `D15`'s dependency is done. | `M76` | 6982 | *hub files only* |
| `backlog-triage.php` harvests no `.ps1` path, so `deploy.ps1` has no edge in the `D13` graph. | `M98` | 10297 | *hub files only* |
| `CLAUDE.md` and `D13` point at a seven-file hub list, but the operative hub set is DERIVED and holds forty-seven. | `M106` | 10950 | *hub files only* |
| `D28`'s chosen option is a no-op as written, and the row that awaits it would implement the illustration. | `M106` | 11090 | *hub files only* |
| The `docs/claims/ | `M85` | 8447 | `resources/public-runtime/lib/db.ts` |
| PHPUnit's own collector loses 40 test files in the container, and a local run reports green without them. | `M76` | 6928 | `tests/Feature/Docs/SuiteCollectionFloorTest.php` · `tests/Support/SourceTree.php` |
| `D13`'s file-overlap rule cannot be satisfied by any pair of rows, because the generated hub set now contains product code. | `M99` | 10528 | `public/index.php` · `resources/views/mail/notification.blade.php` |
| `MdsSegmentedControl` spills 30px INSIDE the builder's config pane — a real horizontal scrollbar, and not the page-overflow defect it was filed as. | `M19` | 630 | `resources/js/components/builder/ConfigPanel.vue` · `packages/design-system/src/components/SegmentedControl/SegmentedControl.vue` · `resources/js/components/integrations/SheetsRuleFields.vue` · `resources/js/components/submissions/FieldInput.vue` |
| The service worker caches a credential-bearing resume shell, and only the RENEWAL of it was closed. | `M70` | 5850 | `resources/public-runtime/sw.ts` · `resources/public-runtime/__tests__/sw.test.ts` · `resources/public-runtime/App.vue` · `resources/public-runtime/__tests__/brand-cache.test.ts` |
| §7.1's flat webhook-delivery paths and §7.4's per-tenant delivery-log UI both contradict the same document's per-endpoint spec, and only the per-endpoint form … | `M80` | 7576 | `app/Http/Controllers/Api/V1/WebhookDeliveryController.php` · `app/Services/Webhooks/WebhookEndpointPresenter.php` · `resources/js/Pages/webhooks/Index.vue` · `app/Enums/WebhookDeliveryStatus.php` +1 |
| A cluster of by-line citations went stale, several of them inside this branch. | `M1` | 5240 | `app/Support/Tenancy/TenantIsolation.php` · `app/Models/Tenant.php` · `scripts/migration-lint.php` · `config/queue.php` +4 |
| `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index … | `M72` | 6342 | — *no file harvested* |
| The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s… | `M75` | 6824 | — *no file harvested* |
| `forms.timezone` is documented as defaulting to NULL and the database defaults it to `'UTC'`. | `M78` | 7315 | — *no file harvested* |
| Two columns are documented under the wrong table in the data dictionary. | `M78` | 7322 | — *no file harvested* |
| Nine live tables have no dictionary section, and they are not all framework scaffolding. | `M78` | 7329 | — *no file harvested* |
| Six `Default` cells describe a column as application-supplied when the database does supply a default, and one of the two words used is factually wrong. | `M83` | 8104 | — *no file harvested* |
| Three `tenants` cells disagree with the live schema on Type and Nullable, and no gate compares either column. | `M83` | 8116 | — *no file harvested* |
| Four citations point into UNTRACKED trees and can never resolve, and nothing separates them from citations that are merely broken. | `M85` | 8424 | — *no file harvested* |
| The mirror gate's comment stripper is line-based and unscoped to `<script>`, so its property grammar on a `.vue` file is first-match-wins across the template a… | `M92` | 9468 | — *no file harvested* |
| A repeated identical builder save failure is announced to a screen reader only once. | `M94` | 9674 | — *no file harvested* |
| The builder toolbar says "All changes saved" on a form that cannot be edited. | `M94` | 9678 | — *no file harvested* |
| The builder's "Not saved" status looks the same as "All changes saved". | `M94` | 9682 | — *no file harvested* |
| Discarding a conflicted offline response uses the browser's plain confirm box. | `M94` | 9697 | — *no file harvested* |
| In an installed offline form, opening another form's queued response leaves the installed window. | `M94` | 9702 | — *no file harvested* |
| Reviewing an offline conflict reopens a form that has since closed or filled up, and the resubmit is then refused. | `M94` | 9707 | — *no file harvested* |
| The password-strength requirement list loses its list semantics in Safari, and 21 more files carry the same shape. | `M94` | 9730 | — *no file harvested* |
| A deploy deletes the previous build's chunks, so an open tab can fail to load a script it needs. | `M96` | 9937 | — *no file harvested* |
| Seven list pages hand-roll the same one-line hint above a table. | `M96` | 10005 | — *no file harvested* |
| The `email` rule accepts a local part longer than 64 characters. | `M96` | 10011 | — *no file harvested* |
| Two delivery logs page with no loading state. | `M96` | 10030 | — *no file harvested* |
| Pressing Check on a Sheets rule whose tab is not the first re-reads the first tab and drops the rule's bindings. | `M96` | 10035 | — *no file harvested* |

## Latent — real, but each needs a stated precondition first

Not queue work until the precondition holds. The row says what it is.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| Route-model binding resolves BEFORE the three console gates, so a synthetic id 404s from the binding rather than from the middleware a test names. | `M67` | 3594 | *hub files only* |
| `SubmissionFinalizer` is a SECOND writer of `submission_answers.form_version_id`, and it would erase the evidence of a divergence rather than record it. | `M73` | 6503 | *hub files only* |
| The super-admin console has no network-level restriction, and hardening it is recorded as deferred with no row. | `M93` | 9597 | *hub files only* |
| The SSO step-up escape link that `docs/security-threat-model.md` §9 records as owed was never added. | `M93` | 9622 | *hub files only* |
| A background job that waits in the queue longer than six hours is failed without ever running. | `M94` | 9691 | *hub files only* |
| `queue:work --timeout` cannot fire on Windows. | `M95` | 9780 | *hub files only* |
| No path revokes `is_super_admin`. | `M95` | 9825 | *hub files only* |
| The refresh job's lock is released before its write commits. | `M95` | 9833 | *hub files only* |
| A deploy that fails before the site comes back up leaves a copy of the server's `.env` in the staging worktree. | `M96` | 10048 | *hub files only* |
| The checklist creates the app, PHP, NSSM and runner folders directly under `C:\` and never restricts their permissions. | `M98` | 10194 | *hub files only* |
| The deploy runner holds Modify on the tree that SYSTEM services execute, and any NETWORK SERVICE process can control the queue worker. | `M98` | 10210 | *hub files only* |
| The runbook's custom-domain arm prescribes nginx, IIS and an HTTP-01 challenge on a box that runs Apache with port 80 closed. | `M98` | 10224 | *hub files only* |
| The pre-push guard's claim rule is a substring test, so a push from a detached HEAD passes it. | `M98` | 10284 | *hub files only* |
| `users.last_active_tenant_id` has no writer anywhere in `app/`. | `M8` | 2606 | `app/Support/Tenancy/TenantExtractColumns.php` |
| `baselineOf()` turns "no checksum" into `''`, and only middleware turns it back. | `M31` | 3106 | `tests/Feature/Submissions/SubmissionEditRoutesTest.php` |
| `gamification:backfill --sync` reports failure after it has already committed every award. | `M32` | 4047 | `app/Console/Commands/BackfillGamificationCommand.php` |
| A line-splitting regex matches a byte INSIDE a UTF-8 character, and one faker name is enough to trigger it. | `M42` | 4175 | `tests/Feature/Audit/ImpersonationAttributionTest.php` |
| ADR-0007 §D11 describes three queue connections that `config/queue.php` does not define, and asserts they are annotated as forbidden when they were deleted. | `M80` | 7766 | `config/queue.php` |
| The documented guest per-IP rate limit of 100/min has no definition — the ceiling on the surface that row describes is 60. | `M80` | 7784 | `config/guest.php` |
| No runbook rotates `APP_KEY`; rotation is only named as a manual step. | `M93` | 9591 | `.env.example` |
| The expression engine bounds parse depth, memo size, length and token count, and has no evaluation-time budget. | `M93` | 9626 | `app/Services/Expressions/ExpressionParser.php` |
| The forms list loads every form a member can see in one request, with no pagination. | `M94` | 9640 | `app/Services/Forms/FormPresenter.php` |
| The seeders' zero-row guard is blind to a role that does not bypass RLS. | `M95` | 9844 | `database/seeders/E2eSeeder.php` |
| stancl resolves a multi-level host differently from `PlatformHost`. | `M95` | 9853 | `app/Support/Tenancy/PlatformHost.php` |
| Airtable computed fields can be mapped as columns a rule writes. | `M96` | 9913 | `app/Support/Connectors/Providers/AirtableConnector.php` |
| The central-host redirect is an absolute `APP_URL`, which a request from another origin cannot follow. | `M96` | 9928 | `tests/e2e/global-setup.ts` |
| `MdsDataTable`'s client-side sort has no mounted test. | `M96` | 9998 | `packages/design-system/src/components/DataTable/DataTable.test.ts` |
| The deploy workflow ships `origin/main`, not the commit whose CI run triggered it. | `M98` | 10138 | `.github/workflows/deploy.yml` |
| `deploy.yml`'s `workflow_run` filter matches a fork pull request whose head branch is named `main`. | `M98` | 10269 | `.github/workflows/deploy.yml` |
| A verified SSO email domain is trusted indefinitely — there is no re-verification sweep. | `M18` | 2410 | `app/Services/Tenancy/CustomDomainService.php` · `app/Jobs/Maintenance/VerifyCustomDomainsJob.php` |
| The database's uniqueness domain and the runtime's resolution domain now disagree about case, and `FormSlug::isTaken()` is the third resolver. | `M61` | 4942 | `app/Support/Forms/FormSlug.php` · `app/Http/Requests/Forms/UpdateFormShareRequest.php` |
| Structured JSON application logs are documented in the present tense and `config/logging.php` has no JSON formatter on any channel. | `M80` | 7701 | `config/logging.php` · `docs/observability-incident-response.md` |
| `export_artifact` objects are documented as auto-deleted seven days after generation, and no scheduled cleanup task is declared. | `M80` | 7753 | `app/Enums/AttachmentKind.php` · `app/Services/Submissions/SubmissionPdfStorage.php` |
| Two more callers of the snapshot serializer still read a torn tree, and whether they should follow `M90`'s instrument is an open decision rather than an oversi… | `M90` | 9190 | `app/Services/Xlsform/XlsformExporter.php` · `app/Models/FieldLibrary.php` |
| A webhook retry or redeliver is signed with the event's original time, so a receiver that enforces a five-minute replay window rejects every attempt sent after… | `M94` | 9631 | `app/Jobs/Webhooks/DeliverWebhookJob.php` · `app/Support/Webhooks/WebhookSigner.php` |
| The Sheets adapter's first-tab fallback is `Sheet1`, and the directory's is the first tab. | `M96` | 9953 | `app/Support/Connectors/Providers/GoogleSheetsDirectory.php` · `app/Support/Connectors/Providers/GoogleSheetsConnector.php` |
| API v1 must send a mapping fingerprint in an undocumented format, and a blank heading is refused there. | `M96` | 9977 | `app/Support/Connectors/SubscriptionConfigRules.php` · `app/Support/Mapping/ColumnFingerprint.php` |
| Google sign-in cannot work on the testing layout, because its redirect URI is built from `APP_URL`, which is the agency's website. | `M98` | 10235 | `config/services.php` · `app/Http/Controllers/Auth/GoogleCallbackController.php` |
| `throttle:saml-acs`'s route BINDING is asserted by nothing, while its registration is. | `M30` | 3364 | `tests/Feature/Sso/SsoLoginWebTest.php` · `tests/Feature/Sso/SsoAcsWebTest.php` · `tests/Feature/Auth/RateLimiterBindingTest.php` |
| The per-endpoint `include_answers: true` webhook payload opt-in has no key anywhere — and four other files appear to record it as a deferral already taken. | `M80` | 7671 | `docs/data-privacy-gdpr-compliance.md` · `docs/piping-output-encoding-design.md` · `config/webhooks.php` |
| `BlueprintValidator` enforces no key FORMAT at all, and `SchemaTreeCloner` carries keys forward verbatim — defence in depth rather than a live defect, and it s… | `M91` | 9343 | `app/Services/Forms/BlueprintValidator.php` · `app/Services/Forms/SchemaTreeCloner.php` · `app/Services/Forms/TemplateService.php` |
| An SSO sign-in whose provisioning hits a missing role is answered with a redirect back towards the identity provider rather than the uniform refusal. | `M93` | 9615 | `app/Http/Controllers/Tenant/Sso/SsoAcsController.php` · `app/Services/Sso/SsoAuthenticationException.php` · `app/Exceptions/Tenancy/MembershipException.php` |
| 26 foreign keys can point at another workspace's rows, and 20 of them would cascade a delete across workspaces. | `M94` | 9716 | `app/Support/Tenancy/ConstraintBoundaries.php` · `docs/adr/0002-multi-tenancy-shared-db-rls.md` · `scripts/constraint-boundary-lint.php` |
| No standing gate proves compatibility with the testing server's PostgreSQL 15. | `M95` | 9805 | `docker-compose.yml` · `app/Support/Analytics/AnalyticsQuery.php` · `app/Services/Analytics/AnalyticsMetricsService.php` |
| Offset-paginated logs order by a second-precision timestamp with no unique tie-breaker. | `M96` | 9960 | `app/Services/Webhooks/WebhookEndpointPresenter.php` · `app/Services/Connectors/ConnectionPresenter.php` · `app/Services/Feedback/FeedbackPresenter.php` |
| Search and the sidebar hide plan-gated destinations when no plan catalog is seeded, while the routes serve them. | `M94` | 9651 | `app/Support/Search/DestinationCatalog.php` · `app/Services/Entitlements/EntitlementService.php` · `app/Http/Middleware/RequireFeature.php` · `app/Support/Entitlements/FeatureAdmission.php` |
| Two more middlewares drop a non-GET payload through `Redirector::guest`, and one of them records no intended URL at all. | `M99` | 10541 | `app/Http/Middleware/RequireRecentPassword.php` · `app/Http/Middleware/EnsureVerifiedEmail.php` · `app/Http/Middleware/EnsureSuperAdminMfa.php` · `app/Http/Middleware/HandleInertiaRequests.php` |
| Correlation IDs threaded through every log line — `request_id` and `job_chain_id` — have no mechanism at all. | `M80` | 7717 | `docs/observability-incident-response.md` · `app/Support/Tenancy/TenantContext.php` · `app/Models/SsoAuthRequest.php` · `app/Services/Sso/SsoAuthRequestService.php` +1 |
| The monthly usage reconciliation dates its period with naive strings and no stated zone. | `M98` | 10324 | `app/Jobs/Entitlements/ReconcileTenantUsageJob.php` · `config/database.php` · `tests/Feature/Tenancy/ConnectionTopologyTest.php` · `config/app.php` +2 |
| Twelve existing tenant-context call sites restore in a `finally` INSIDE their transaction, which is the shape `TenantContext::runFor()` was deliberately built … | `M3` | 734 | `app/Support/Tenancy/TenantContext.php` · `app/Listeners/Auth/SendWelcomeEmail.php` · `app/Services/Admin/ImpersonationService.php` · `app/Services/Admin/SuperAdminService.php` +3 |
| The framework's own mail components interpolate into attributes the same way, and the M57 gate cannot reach them. | `M57` | 4676 | `scripts/mail-attribute-lint.php` · `app/Support/Tenancy/TenantUrl.php` · `resources/views/mail/notification.blade.php` |
| Five screen-reader-only live regions have no positioned ancestor inside their component. | `M94` | 9723 | `resources/js/components/builder/BuilderCanvas.vue` · `resources/js/components/shell/FeedbackButton.vue` · `resources/js/components/submissions/GeoInput.vue` · `resources/public-runtime/components/RuntimeShell.vue` +1 |
| M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t… | `M5` | 836 | — *no file harvested* |
| A 5xx that arrives AFTER the provider committed is still re-driven. | `M5` | 849 | — *no file harvested* |
| A rotated token can still be lost in the one-UPDATE window M6 left. | `M6` | 901 | — *no file harvested* |
| In-app notifications are never pruned, and "Mark all as read" is one unbounded update. | `M94` | 9686 | — *no file harvested* |
| A worker relaunched by NSSM inside the deploy window starts on a half-swapped checkout. | `M96` | 9945 | — *no file harvested* |
| bcrypt reads only a password's first 72 bytes. | `M96` | 10018 | — *no file harvested* |
| A previous release's `vendor` that cannot be deleted stops every later deploy before its window. | `M96` | 10055 | — *no file harvested* |
| The workspaces page prescribed for the central-host loop cannot be built: `pgsql_auth` has no grant on `tenants` or `domains`. | `M99` | 10519 | — *no file harvested* |

## Not live — listed, not hidden

These are stated limits, deliberate decisions, blind spots and deployment obligations
rather than defects in this tree. `scripts/loop.php` already refuses them, and they are
listed here rather than dropped so that this file and that stop rule cannot disagree.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| Four `DO NOT RE-ASK` user decisions of record now live in `PROGRESS_ARCHIVE.md` rather than in `docs/claims/decisions.md`. | `M45` | 5416 | *hub files only* |
| The data dictionary names nine of the forty-five constraints the migrations declare, while enumerating constraints exhaustively in places. | `M46` | 5051 | *hub files only* |
| The citation-liveness gate cannot see a behaviour negative, and its ledger ceiling counts deliberately-preserved historical filings. | `M46` | 5059 | *hub files only* |
| `routes/tenant.php`'s ~95 `can:` gates get none of M63's checks, and the derived half is expected to FIND something. | `M63` | 3706 | *hub files only* |
| `docs/backlog-triage.md` keeps a tier-1 citation exemption whose stated reason stopped being true in the same increment. | `M65` | 3808 | *hub files only* |
| Making the repository private breaks the server's fetch, starts billing the CI minutes and may silently disarm the merge gate. | `M98` | 10250 | *hub files only* |
| The sync surface's read and write are gated on different permission families, so no single non-admin role can complete the offline loop. | `M13` | 1927 | `app/Support/Api/ApiAbilities.php` |
| A semantic token is no guarantee of a visible element, and one more instance is probably out there. | `M23` | 3011 | `resources/js/components/builder/LogicRail.vue` |
| The liveness marker is gated for presence and nothing checks that a verdict is CORRECT — and the error rate of judging one is now measured rather than assumed. | `M65` | 3819 | `tests/Feature/Docs/BacklogProvenanceTest.php` |
| Four P3a refusal cases assert the exception CLASS and never the message. | `M12` | 2006 | `tests/Feature/Submissions/SubmissionDraftServiceTest.php` · `app/Exceptions/Submissions/SubmissionConflictException.php` |
| `gamification:backfill --dry-run` cannot reveal a mis-scoring defect, by construction. | `M24` | 2107 | `app/Services/Gamification/BackfillTally.php` · `app/Console/Commands/BackfillGamificationCommand.php` |
| the dashboard card ranks the whole tenant to compute three numbers that need no ranking. | `M26` | 2132 | `app/Http/Controllers/Tenant/DashboardController.php` · `app/Services/Gamification/LeaderboardService.php` |
| Two SSO test files justify a real assertion with a rationale that is false on this framework version. | `M30` | 3374 | `tests/Feature/Sso/SsoLoginWebTest.php` · `tests/Feature/Sso/SsoLoginCompletionWebTest.php` |
| Any tenant-scoped policy read mounted on the Fortify group is silently blind, and two increments have now walked into it. | `M68` | 2692 | `app/Http/Middleware/EstablishTenantDatabaseContext.php` · `app/Services/Settings/TenantSettingRegistry.php` · `app/Services/Settings/RegistrationGate.php` · `app/Services/Settings/TwoFactorEnforcementGate.php` |
| Thirteen Vitest stubs across four files are silently inert. | `M23` | 2998 | `resources/js/Pages/submissions/show.test.ts` · `resources/js/components/sso/cards.test.ts` · `resources/js/components/sso/SsoPolicyCard.test.ts` · `resources/js/Layouts/AppLayout.test.ts` +2 |
| Every `MaintenanceJob` fan-out is proved one file at a time, so a future one inherits no coverage. | `M44` | 4008 | `app/Jobs/MaintenanceJob.php` · `app/Jobs/Maintenance/VerifyCustomDomainsJob.php` · `app/Jobs/Maintenance/PruneFailedJobsJob.php` · `tests/Feature/Queue/JobContractTest.php` +12 |
| The command gate reads `README.md` only, and three other documents carry runnable command blocks. | `M59` | 4900 | *hub files only* |
| Nothing checks that a `§D<n>` citation names a section whose text supports it. | `M7` | 5225 | — *no file harvested* |

## Hub files — a batch may touch at most one

Derived, not listed: a file cited by **3 or more** open rows. These are
mostly meta-files rather than product code, which is why the rows that share them are not
really coupled — and why excluding them is what makes the queue above separable.

| File | Open rows citing it |
|---|---|
| `docs/deployment-infrastructure.md` | 19 |
| `.github/workflows/ci.yml` | 17 |
| `docs/security-threat-model.md` | 16 |
| `scripts/backlog-triage.php` | 15 |
| `scripts/state.php` | 14 |
| `docs/data-dictionary.md` | 14 |
| `routes/tenant.php` | 14 |
| `CLAUDE.md` | 13 |
| `scripts/citation-liveness-lint.php` | 11 |
| `docs/backlog-triage.md` | 10 |
| `docs/pipeline.md` | 10 |
| `routes/api.php` | 10 |
| `PROGRESS_ARCHIVE.md` | 9 |
| `docs/feature-backlog.md` | 9 |
| `scripts/pipeline.php` | 9 |
| `PROGRESS.md` | 8 |
| `docs/claims/decisions.md` | 8 |
| `openapi.json` | 8 |
| `docs/api-specification.md` | 8 |
| `bootstrap/app.php` | 8 |
| `scripts/mutate.php` | 7 |
| `docs/claims/lane-a.md` | 7 |
| `scripts/next.php` | 6 |
| `composer.json` | 6 |
| `docs/gate-baselines.md` | 5 |
| `scripts/loop.php` | 5 |
| `docs/adr/0007-async-execution-substrate.md` | 5 |
| `app/Services/Forms/FormService.php` | 5 |
| `config/fortify.php` | 5 |
| `docs/PRD.md` | 4 |
| `docs/ACCESS-MATRIX.md` | 4 |
| `app/Services/Forms/FormBuilderService.php` | 4 |
| `scripts/test-pointer-lint.php` | 4 |
| `app/Services/Sso/SsoDomainService.php` | 4 |
| `docs/architecture/technical-architecture.md` | 4 |
| `docs/webhook-integration-design.md` | 4 |
| `app/Jobs/Connectors/RefreshOneConnectionJob.php` | 4 |
| `app/Providers/AppServiceProvider.php` | 4 |
| `app/Services/Tenancy/TenantMembershipService.php` | 4 |
| `docs/claims/lane-b.md` | 3 |
| `docs/ux/design-system-reference.md` | 3 |
| `docs/ux/exceptions-log.md` | 3 |
| `scripts/tracker-surgery.php` | 3 |
| `scripts/pre-push-guard.php` | 3 |
| `tests/e2e/public-runtime-offline.spec.ts` | 3 |
| `scripts/tracker-lint.php` | 3 |
| `app/Services/Submissions/FormAcceptanceGuard.php` | 3 |
| `scripts/gate-baselines.php` | 3 |
| `docs/non-functional-requirements.md` | 3 |
| `app/Services/Submissions/SubmissionDraftService.php` | 3 |
| `app/Models/Form.php` | 3 |
| `database/migrations/2026_07_06_000201_create_forms_table.php` | 3 |
| `app/Services/Submissions/SubmissionFinalizer.php` | 3 |
| `docs/offline-first-sync-design.md` | 3 |
| `app/Services/Forms/PublishService.php` | 3 |
| `database/seeders/DemoSeeder.php` | 3 |
| `routes/console.php` | 3 |
| `docs/TESTING-GUIDE.md` | 3 |
| `app/Jobs/TenantAwareJob.php` | 3 |

## What to take next

Not here. **`docs/pipeline.md` § Next** names the work, ordered by tier and then by readiness — one
picker, not two. This file keeps its own job: the operability ranking inside the defect ledger, and
the hub set above, which `D13` still uses to group the rows of one tier into an increment.

## What this file cannot see

**30 open row(s) cite no file this script could resolve**, and **17 carry at least one citation that does not resolve.** They are ranked below rows whose citations
all resolve, because a row must be re-derived before it is worked rather than merely worked —
but the ranking is the only consequence: an unresolvable citation is not evidence the row is
wrong. **Batch these by hand**, since a collision check needs files to compare.

Rows with no harvested file:

- `docs/feature-backlog.md:6342` — `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index …
- `docs/feature-backlog.md:6824` — The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s…
- `docs/feature-backlog.md:7315` — `forms.timezone` is documented as defaulting to NULL and the database defaults it to `'UTC'`.
- `docs/feature-backlog.md:7322` — Two columns are documented under the wrong table in the data dictionary.
- `docs/feature-backlog.md:7329` — Nine live tables have no dictionary section, and they are not all framework scaffolding.
- `docs/feature-backlog.md:8104` — Six `Default` cells describe a column as application-supplied when the database does supply a default, and one of the two words used is factually wrong.
- `docs/feature-backlog.md:8116` — Three `tenants` cells disagree with the live schema on Type and Nullable, and no gate compares either column.
- `docs/feature-backlog.md:8424` — Four citations point into UNTRACKED trees and can never resolve, and nothing separates them from citations that are merely broken.
- `docs/feature-backlog.md:9468` — The mirror gate's comment stripper is line-based and unscoped to `<script>`, so its property grammar on a `.vue` file is first-match-wins across the template a…
- `docs/feature-backlog.md:9674` — A repeated identical builder save failure is announced to a screen reader only once.
- `docs/feature-backlog.md:9678` — The builder toolbar says "All changes saved" on a form that cannot be edited.
- `docs/feature-backlog.md:9682` — The builder's "Not saved" status looks the same as "All changes saved".
- `docs/feature-backlog.md:9697` — Discarding a conflicted offline response uses the browser's plain confirm box.
- `docs/feature-backlog.md:9702` — In an installed offline form, opening another form's queued response leaves the installed window.
- `docs/feature-backlog.md:9707` — Reviewing an offline conflict reopens a form that has since closed or filled up, and the resubmit is then refused.
- `docs/feature-backlog.md:9730` — The password-strength requirement list loses its list semantics in Safari, and 21 more files carry the same shape.
- `docs/feature-backlog.md:9937` — A deploy deletes the previous build's chunks, so an open tab can fail to load a script it needs.
- `docs/feature-backlog.md:10005` — Seven list pages hand-roll the same one-line hint above a table.
- `docs/feature-backlog.md:10011` — The `email` rule accepts a local part longer than 64 characters.
- `docs/feature-backlog.md:10030` — Two delivery logs page with no loading state.
- `docs/feature-backlog.md:10035` — Pressing Check on a Sheets rule whose tab is not the first re-reads the first tab and drops the rule's bindings.
- `docs/feature-backlog.md:836` — M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t…
- `docs/feature-backlog.md:849` — A 5xx that arrives AFTER the provider committed is still re-driven.
- `docs/feature-backlog.md:901` — A rotated token can still be lost in the one-UPDATE window M6 left.
- `docs/feature-backlog.md:9686` — In-app notifications are never pruned, and "Mark all as read" is one unbounded update.
- `docs/feature-backlog.md:9945` — A worker relaunched by NSSM inside the deploy window starts on a half-swapped checkout.
- `docs/feature-backlog.md:10018` — bcrypt reads only a password's first 72 bytes.
- `docs/feature-backlog.md:10055` — A previous release's `vendor` that cannot be deleted stops every later deploy before its window.
- `docs/feature-backlog.md:10519` — The workspaces page prescribed for the central-host loop cannot be built: `pgsql_auth` has no grant on `tenants` or `domains`.
- `docs/feature-backlog.md:5225` — Nothing checks that a `§D<n>` citation names a section whose text supports it.
