# Backlog triage

**Generated by `scripts/backlog-triage.php`. Do not hand-edit — regenerate it.**

**Measured 2026-09-06** against `origin/main` at `4ed7e7108b45a21692a266634459761c9f2eabad` · 119 open rows · 0 `major` · 119 `minor` · 0 `nit`

⚠️ **This is an OPERABILITY order, not a priority order, and the difference is why the file it
replaced went wrong.** It cannot tell you what matters most. Severity would, and there are zero
open `major` rows, so that field separates nothing. What it derives is which rows are still
**live**, whose **citations still resolve**, and which rows **share no files** with each other —
which is what picking a batch needs, and is all this file claims.

⛔ **Liveness here is a marker a human wrote, not a measurement this script made.** It is gated
for presence by `tests/Feature/Docs/BacklogProvenanceTest.php` and reported by
`scripts/state.php`; neither checks that a verdict is *right*. A row marked live that is dead
passes everything. The mutation harness is what settles a row, and settling it is the job of
whichever increment takes it.

📌 **The 2026-08-28 census this file replaced is preserved byte-for-byte in `docs/backlog-triage-m37.md`.**
Its findings about the sweep's own accuracy, and the brief it ran under, are not reproduced here
and are not stale — they are a dated record and are still worth reading before any fan-out.

---

## Census

| | Rows |
|---|---|
| Open | **119** |
| — still `live` | **69** |
| — `latent`, needs a stated precondition | 24 |
| — `not live`, not a defect in this tree | 26 |
| Severity bullets, ever | 254 |

## The queue — live rows, most operable first

Ordered by citation health, then by how few non-hub files the row touches, then oldest
debt first. `cites` lists only the files a batch would collide on; hub files are excluded
from it and listed separately below.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| `scripts/mutate.php` cannot drive a positive control for anything that is not Pest in a container. | `M42` | 4281 | *hub files only* |
| `submission_geo_index` is a real table and the data dictionary does not mention it at all. | `M58` | 4676 | *hub files only* |
| `docs/data-dictionary.md` documents nine `tenants` columns that exist in no migration, and its enum catalog contradicts the enum. | `M72` | 6167 | *hub files only* |
| The `npm audit` judge makes a REQUIRED context green while nothing was measured, and that is a deliberate trade rather than an oversight. | `M72` | 6216 | *hub files only* |
| What actually delivers the offline mis-cased render is unknown, and TWO confident models of it have now been wrong. | `M73` | 6230 | *hub files only* |
| The triage generator silently drops any citation written as a PARTIAL path, and a partial path is strictly worse than a bare filename. | `M73` | 6302 | *hub files only* |
| The public runtime's media pick path has ZERO test coverage of any kind, and no e2e seeded form has a media field at all. | `M78` | 7024 | *hub files only* |
| The documented-literal drift gate needs three normalizer rules, not "a normalizer per type", and one of the row's two justifications is fabricated. | `M78` | 7065 | *hub files only* |
| The standing `pgsql_auth` rule was never literally true, and its stated revisit trigger names a different grant. | `M78` | 7094 | *hub files only* |
| A claim file has no constrained form for a forward declaration, so the one stale declaration on the tree cannot be gated. | `M42` | 4154 | `docs/claims/lane-b.md` |
| `tracker-lint` R8 guards `CLAUDE.md` and cannot reach `PROGRESS.md`, which is the half that actually rotted. | `M42` | 4166 | `PROGRESS.md` |
| The documented-default gate reads FUNCTION-shaped cells only, so a documented LITERAL that disagrees with the database is invisible to it. | `M58` | 4664 | `tests/Feature/Migrations/DocumentedDefaultDriftTest.php` |
| The audit spec's §1 table is asserted by nothing, and a static sweep cannot be the thing that asserts it. | `M70` | 5797 | `docs/audit-compliance-logging-spec.md` |
| `R7`'s byte threshold was calibrated on surgeries that were all left too late, so acting EARLY lands in its dead zone. | `M71` | 5854 | `scripts/tracker-surgery.php` |
| `docs/backlog-triage.md` is generated stale by its own close-out, and the drift is on the trunk now. | `M71` | 5946 | `docs/feature-backlog.md` |
| `scripts/next.php` never clips the release HEADING, so a third of the hand-off is byte-identical noise. | `M72` | 6199 | `scripts/next.php` |
| A correction still cannot be autosaved, and the reason is an endpoint that does not exist plus a product decision nobody has taken. | `M75` | 6723 | `resources/js/Pages/submissions/Encode.vue` |
| The data-dictionary row's CHECK-constraint census is wrong, and the correction is the opposite of a tidy-up. | `M77` | 6918 | `app/Support/Migrations/PublishedVersionGuard.php` |
| The storage-quota row's stated blocker is not real, and its re-aim needs a copy decision nobody has made. | `M77` | 6963 | `resources/public-runtime/__tests__/sync-status.test.ts` |
| `public-runtime-offline.spec.ts`'s parked-conflict case is FLAKY across viewport projects, and `D2` makes that a merge-blocking property. | `M77` | 6979 | `resources/public-runtime/sw.ts` |
| `preserveReviewedAnswers()` writes a media reference it has already deleted, in seconds, with no grace window. | `M78` | 7000 | `resources/public-runtime/components/RuntimeSession.vue` |
| `reap.test.ts` leaves the mark set's status list unpinned, so a third of it can be deleted with all 14 cases green. | `M78` | 7016 | `resources/public-runtime/__tests__/reap.test.ts` |
| `scripts/loop.php --assess` does not read `docs/pipeline.md`, so it cannot refuse a row on its PIPELINE state. | `M79` | 5850 | `docs/pipeline.md` |
| A pipeline marker placed mid-document SILENTLY INVALIDATES every `path:N` citation beneath it, and only the ones that land on a blank line are caught. | `M79` | 5852 | `docs/adr/0008-entitlement-and-metering.md` |
| §7.1 names a `/api/v1/webhooks/endpoints` path segment that exists nowhere in the tree, and it is a floor rather than a census. | `M80` | 7284 | `tests/Feature/Webhooks/WebhookEndpointApiTest.php` |
| `scripts/next.php` takes each release's LEAD paragraph, and a lead paragraph is often a file manifest rather than the lesson. | `M42` | 4301 | `scripts/next.php` · `docs/claims/TEMPLATE.md` |
| A tracker surgery whose diff touches only `paths-ignore`d files produces no post-merge run at all, so `R7`'s trunk arm is unreachable for exactly the diff shap… | `M48` | 5399 | `PROGRESS.md` · `docs/claims/decisions.md` |
| `docker compose run --rm e2e` does not work on this host as three documents prescribe it, and the most likely wrong form EXITS 0. | `M61` | 4897 | `docs/ux/design-system-reference.md` · `docs/ux/exceptions-log.md` |
| `CLAUDE.md`'s gate table sends PHPStan to the container, one row below the rule that explains why the container is wrong. | `M76` | 6739 | `scripts/gate-baselines.php` · `database/migrations/2026_07_06_000205_create_form_fields_table.php` |
| Two user-supplied predicates now run on the `pgsql_auth` connection, against a standing rule that says none may. | `M77` | 6865 | `docs/multi-tenancy-rbac-design.md` · `docs/adr/0002-multi-tenancy-shared-db-rls.md` |
| §7.1's `Form draft` row pins four `/api/v1` builder endpoints registered nowhere, and the `validations` sub-resource exists on neither surface. | `M80` | 7214 | `docs/xlsform-interop-spec.md` · `app/Services/Forms/FormBuilderService.php` |
| `forms.allow_manual_encoding` is documented as Feature #7's capability flag and has neither a reader nor a writer — the only one of five inert `allow_*` flags … | `M80` | 7134 | `tests/Feature/Tenancy/TenantExtractColumnDriftTest.php` · `app/Policies/SubmissionPolicy.php` · `app/Services/Forms/FormService.php` |
| The documented async export API — `POST /api/v1/forms/{form}/exports` and `GET /api/v1/exports/{export}` — has zero routes and no `exports` job row, while the … | `M80` | 7177 | `app/Enums/QueueName.php` · `app/Jobs/Submissions/GeneratePdfJob.php` · `config/queue-fairness.php` |
| `docs/api-specification.md:63` states in the present tense that every unsafe request is deduplicated against a 24-hour Redis cache keyed on `(tenant_id, endpoi… | `M80` | 7330 | `app/Services/Submissions/SubmissionPipeline.php` · `app/Services/Webhooks/WebhookEventDispatcher.php` · `app/Services/Gamification/PointsRecorder.php` |
| The documented `Users & roles` API resource group (`GET/POST /api/v1/users`, `/api/v1/roles`) has zero routes, and a shipped schema decision was already paid f… | `M80` | 7197 | `app/Http/Resources/Api/V1/AuditResource.php` · `docs/multi-tenancy-rbac-design.md` · `app/Models/Role.php` · `config/permission.php` |
| Two more scripts have `backlog-triage.php`'s destructive default, and the row that filed that defect names one of them as its harmless counter-example. | `M71` | 5900 | `scripts/gate-baselines.php` · `scripts/regenerate-brand-ramp-fixture.php` · `tests/fixtures/brand-ramp.json` · `scripts/npm-audit-judge.php` +1 |
| `forms.single_page_mode` has no write surface outside the seeders, so single-page mode is unreachable for a real tenant — and its documented default disagrees … | `M80` | 7112 | `app/Services/Submissions/EncodeFormPresenter.php` · `app/Services/Submissions/PublicFormPresenter.php` · `database/seeders/DemoSeeder.php` · `database/seeders/E2eSeeder.php` +5 |
| `forms.allow_offline_sync` has no reader anywhere — neither the sync manifest nor the PWA install entry honours the per-form offline gate that two documents de… | `M80` | 7156 | `tests/Feature/Tenancy/TenantExtractColumnDriftTest.php` · `docs/erd.md` · `docs/ux/form-filling-ux-flow.md` · `app/Http/Controllers/Api/V1/SyncManifestController.php` +5 |
| NFR §8 sets a 30-day soft-delete grace period before a hard-deletion job, and for the three entities it names there is no purge job, no grace-period config val… | `M80` | 7301 | `docs/non-functional-requirements.md` · `app/Jobs/Maintenance/PruneFailedJobsJob.php` · `app/Jobs/Submissions/ReapTenantDraftsJob.php` · `app/Services/Submissions/ClientUuidResolver.php` +5 |
| The parity matrix scores API/programmatic import as Phase-1 shipped, and the code's own enum docblock calls it a later channel. | `M80` | 7235 | `docs/competitive-feature-parity-matrix.md` · `app/Services/Submissions/SubmissionDraftService.php` · `app/Http/Controllers/Api/V1/SyncSubmissionController.php` · `app/Http/Controllers/Public/GuestDraftController.php` +6 |
| Three gate invocations fetch `http-server` from the network at run time, and nothing declares it. | `M59` | 4795 | *hub files only* |
| `scripts/backlog-triage.php --check` is wired into nothing, so the generated triage can drift with no gate saying so. | `M65` | 3709 | *hub files only* |
| `M59`'s undeclared-`http-server` row undercounts on both of its numbers. | `M71` | 5993 | *hub files only* |
| The partial-path citation row's consequences are mostly false, and the measurement that settles `D15`'s dependency is done. | `M76` | 6822 | *hub files only* |
| PHPUnit's own collector loses 40 test files in the container, and a local run reports green without them. | `M76` | 6768 | `tests/Feature/Docs/SuiteCollectionFloorTest.php` |
| A media pick made during a conflict review is protected only by the reaper's grace window. | `M22` | 1353 | `resources/public-runtime/lib/reap.ts` · `tsconfig.sw.json` |
| `MdsSegmentedControl` spills 30px INSIDE the builder's config pane — a real horizontal scrollbar, and not the page-overflow defect it was filed as. | `M19` | 630 | `resources/js/components/builder/ConfigPanel.vue` · `packages/design-system/src/components/SegmentedControl/SegmentedControl.vue` · `docs/ux/exceptions-log.md` |
| The service worker caches a credential-bearing resume shell, and only the RENEWAL of it was closed. | `M70` | 5751 | `resources/public-runtime/sw.ts` · `resources/public-runtime/__tests__/sw.test.ts` · `docs/offline-first-sync-design.md` · `docs/non-functional-requirements.md` +1 |
| §7.1's flat webhook-delivery paths and §7.4's per-tenant delivery-log UI both contradict the same document's per-endpoint spec, and only the per-endpoint form … | `M80` | 7260 | `app/Http/Controllers/Api/V1/WebhookDeliveryController.php` · `app/Services/Webhooks/WebhookEndpointPresenter.php` · `resources/js/Pages/webhooks/Index.vue` · `app/Enums/WebhookDeliveryStatus.php` +1 |
| A cluster of by-line citations went stale, several of them inside this branch. | `M1` | 5141 | `app/Support/Tenancy/TenantIsolation.php` · `app/Models/Tenant.php` · `scripts/migration-lint.php` · `config/queue.php` +7 |
| A tabular rule that maps no Submission ID column still duplicates on an unconfirmed retry, and the durable fix is in the rule EDITOR rather than the adapter. | `M5` | 795 | — *no file harvested* |
| `SlackConnector::deliver()` has the same non-idempotent shape and is deliberately not covered. | `M5` | 830 | — *no file harvested* |
| `promote()` re-asserts the version is published BEFORE the lock and never again under it. | `M12` | 1912 | — *no file harvested* |
| The tenant-facing SSO domains card on `/settings/sso` does not exist, so verification is operator-assisted. | `M18` | 2376 | — *no file harvested* |
| `MemberController::invite()` validates `['required', 'email', 'max:255']` and a role, with no domain-ownership check. | `M18` | 2391 | — *no file harvested* |
| Self-registration remains a way to occupy an address in a domain you do not control. | `M18` | 2402 | — *no file harvested* |
| The storage-quota line counts strangers' submissions. | `M21` | 1381 | — *no file harvested* |
| Every hand-off prescribes a Pint command that scans ~40 fewer files than CI does. | `M36` | 4058 | — *no file harvested* |
| `PUT /user/profile-information` is a second mail cannon, and it is the one Fortify write route left deliberately unbound. | `M43` | 3262 | — *no file harvested* |
| The promote route still documents neither of its two 403 causes nor its 422, and the generator cannot narrow a refusal family to one route. | `M67` | 1941 | — *no file harvested* |
| The per-item `error.code` is the integrator's real branching key on `/sync/submissions`, and the contract still publishes it as an unconstrained string. | `M69` | 1809 | — *no file harvested* |
| `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index … | `M72` | 6182 | — *no file harvested* |
| What a media cell should SAY beyond a filename, and what shape a grid answer takes in a spreadsheet, are two product questions `M74` deliberately did not answe… | `M74` | 6447 | — *no file harvested* |
| The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s… | `M75` | 6664 | — *no file harvested* |
| The data dictionary documents `tenants.status` defaulting to a value that is not a legal case of the enum it names. | `M78` | 7033 | — *no file harvested* |
| `forms.timezone` is documented as defaulting to NULL and the database defaults it to `'UTC'`. | `M78` | 7042 | — *no file harvested* |
| Two columns are documented under the wrong table in the data dictionary. | `M78` | 7049 | — *no file harvested* |
| Nine live tables have no dictionary section, and they are not all framework scaffolding. | `M78` | 7056 | — *no file harvested* |
| `MdsSegmentedControl`'s stretch-clamp census is short by two consumers, one candidate fix is a no-op, and the 30px figure cannot be reproduced. | `M78` | 7079 | — *no file harvested* |

## Latent — real, but each needs a stated precondition first

Not queue work until the precondition holds. The row says what it is.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| A verified SSO email domain is trusted indefinitely — there is no re-verification sweep. | `M18` | 2361 | *hub files only* |
| The setup-time directory has no pre-flight refresh | `M1` | 884 | `app/Services/Connectors/TabularDestinationDirectory.php` |
| `baselineOf()` turns "no checksum" into `''`, and only middleware turns it back. | `M31` | 3057 | `tests/Feature/Submissions/SubmissionEditRoutesTest.php` |
| `gamification:backfill --sync` reports failure after it has already committed every award. | `M32` | 3961 | `app/Console/Commands/BackfillGamificationCommand.php` |
| Route-model binding resolves BEFORE the three console gates, so a synthetic id 404s from the binding rather than from the middleware a test names. | `M67` | 3508 | `bootstrap/app.php` |
| The documented guest per-IP rate limit of 100/min has no definition — the ceiling on the surface that row describes is 60. | `M80` | 7456 | `config/guest.php` |
| A line-splitting regex matches a byte INSIDE a UTF-8 character, and one faker name is enough to trigger it. | `M42` | 4089 | `tests/Feature/Audit/ImpersonationAttributionTest.php` · `docs/claims/lane-a.md` |
| Structured JSON application logs are documented in the present tense and `config/logging.php` has no JSON formatter on any channel. | `M80` | 7373 | `config/logging.php` · `docs/observability-incident-response.md` |
| The API rate-limit table promises 300 requests/minute per authenticated user and no such limiter is defined. | `M80` | 7404 | `app/Providers/FortifyServiceProvider.php` · `app/Providers/QueueServiceProvider.php` |
| ADR-0007 §D11 describes three queue connections that `config/queue.php` does not define, and asserts they are annotated as forbidden when they were deleted. | `M80` | 7438 | `config/queue.php` · `docs/adr/0007-async-execution-substrate.md` |
| `throttle:saml-acs`'s route BINDING is asserted by nothing, while its registration is. | `M30` | 3278 | `tests/Feature/Sso/SsoLoginWebTest.php` · `tests/Feature/Sso/SsoAcsWebTest.php` · `tests/Feature/Auth/RateLimiterBindingTest.php` |
| The per-endpoint `include_answers: true` webhook payload opt-in has no key anywhere — and four other files appear to record it as a deferral already taken. | `M80` | 7355 | `docs/data-privacy-gdpr-compliance.md` · `docs/piping-output-encoding-design.md` · `config/webhooks.php` |
| `export_artifact` objects are documented as auto-deleted seven days after generation, and no scheduled cleanup task is declared. | `M80` | 7425 | `docs/deployment-infrastructure.md` · `app/Enums/AttachmentKind.php` · `app/Services/Submissions/SubmissionPdfStorage.php` |
| "1 concurrent sync export per form, additional requests 429" — no concurrency guard exists on any export path. | `M80` | 7474 | `app/Jobs/Connectors/RefreshOneConnectionJob.php` · `app/Jobs/MaintenanceJob.php` · `app/Services/Submissions/FormAcceptanceGuard.php` |
| Correlation IDs threaded through every log line — `request_id` and `job_chain_id` — have no mechanism at all. | `M80` | 7389 | `docs/observability-incident-response.md` · `app/Models/SsoAuthRequest.php` · `app/Services/Sso/SsoAuthRequestService.php` · `app/Models/SsoAuthFailure.php` |
| Twelve existing tenant-context call sites restore in a `finally` INSIDE their transaction, which is the shape `TenantContext::runFor()` was deliberately built … | `M3` | 708 | `app/Listeners/Auth/SendWelcomeEmail.php` · `app/Services/Admin/ImpersonationService.php` · `app/Services/Admin/SuperAdminService.php` · `app/Services/Auth/GoogleAuthRequestService.php` +3 |
| `lib/brand-cache.ts` is a SECOND writer to `guest-shell-html`, and it renews a mis-cased key with a response a navigation cannot use. | `M72` | 6105 | `resources/public-runtime/__tests__/brand-cache.test.ts` |
| The framework's own mail components interpolate into attributes the same way, and the M57 gate cannot reach them. | `M57` | 4590 | `scripts/mail-attribute-lint.php` · `resources/views/mail/notification.blade.php` |
| M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t… | `M5` | 810 | — *no file harvested* |
| A 5xx that arrives AFTER the provider committed is still re-driven. | `M5` | 823 | — *no file harvested* |
| A rotated token can still be lost in the one-UPDATE window M6 left. | `M6` | 875 | — *no file harvested* |
| `users.last_active_tenant_id` has no writer anywhere in `app/`. | `M8` | 2557 | — *no file harvested* |
| The database's uniqueness domain and the runtime's resolution domain now disagree about case, and `FormSlug::isTaken()` is the third resolver. | `M61` | 4843 | — *no file harvested* |
| `SubmissionFinalizer` is a SECOND writer of `submission_answers.form_version_id`, and it would erase the evidence of a divergence rather than record it. | `M73` | 6343 | — *no file harvested* |

## Not live — listed, not hidden

These are stated limits, deliberate decisions, blind spots and deployment obligations
rather than defects in this tree. `scripts/loop.php` already refuses them, and they are
listed here rather than dropped so that this file and that stop rule cannot disagree.

| Row | Filed | Line | Cites (non-hub) |
|---|---|---|---|
| M8's GRANT removed an accidental backstop that a mutation argument was leaning on. | `M8` | 2548 | *hub files only* |
| Two `WebhookRetrySweepTest` cases were passing for a reason unrelated to their names. | `M44` | 3911 | *hub files only* |
| The data dictionary names nine of the forty-five constraints the migrations declare, while enumerating constraints exhaustively in places. | `M46` | 4952 | *hub files only* |
| `scripts/tracker-lint-controls.php` proves R7 against synthetic histories, and nothing proves it against a REAL GitHub `push` or squash. | `M49` | 5601 | *hub files only* |
| `routes/tenant.php`'s ~95 `can:` gates get none of M63's checks, and the derived half is expected to FIND something. | `M63` | 3620 | *hub files only* |
| The triage generator's collision check harvests citations from row TEXT, so it proposes batches that collide. | `M70` | 5832 | *hub files only* |
| Four P3a refusal cases assert the exception CLASS and never the message. | `M12` | 1957 | `tests/Feature/Submissions/SubmissionDraftServiceTest.php` |
| The delivery-rule modal's channel-refresh button is the same unguarded shape, GET-only. | `M23` | 2942 | `resources/js/components/integrations/RuleFormModal.vue` |
| A semantic token is no guarantee of a visible element, and one more instance is probably out there. | `M23` | 2962 | `resources/js/components/builder/LogicRail.vue` |
| Four `DO NOT RE-ASK` user decisions of record now live in `PROGRESS_ARCHIVE.md` rather than in `docs/claims/decisions.md`. | `M45` | 5317 | `docs/claims/decisions.md` |
| The citation-liveness gate cannot see a behaviour negative, and its ledger ceiling counts deliberately-preserved historical filings. | `M46` | 4960 | `docs/feature-backlog.md` |
| `docs/backlog-triage.md` keeps a tier-1 citation exemption whose stated reason stopped being true in the same increment. | `M65` | 3722 | `scripts/citation-liveness-lint.php` |
| The liveness marker is gated for presence and nothing checks that a verdict is CORRECT — and the error rate of judging one is now measured rather than assumed. | `M65` | 3733 | `tests/Feature/Docs/BacklogProvenanceTest.php` |
| Any tenant-scoped policy read mounted on the Fortify group is silently blind, and two increments have now walked into it. | `M68` | 2643 | `config/fortify.php` |
| `gamification:backfill --dry-run` cannot reveal a mis-scoring defect, by construction. | `M24` | 2058 | `app/Services/Gamification/BackfillTally.php` · `app/Console/Commands/BackfillGamificationCommand.php` |
| Two SSO test files justify a real assertion with a rationale that is false on this framework version. | `M30` | 3288 | `tests/Feature/Sso/SsoLoginWebTest.php` · `tests/Feature/Sso/SsoLoginCompletionWebTest.php` |
| Thirteen Vitest stubs across four files are silently inert. | `M23` | 2949 | `resources/js/Pages/submissions/show.test.ts` · `resources/js/components/sso/cards.test.ts` · `resources/js/components/sso/SsoPolicyCard.test.ts` · `resources/js/Layouts/AppLayout.test.ts` +2 |
| Every `MaintenanceJob` fan-out is proved one file at a time, so a future one inherits no coverage. | `M44` | 3922 | `tests/Feature/Queue/JobContractTest.php` · `app/Jobs/Maintenance/SweepWebhookRetriesJob.php` · `app/Jobs/Maintenance/SweepScheduledFormsJob.php` · `app/Jobs/Maintenance/RollUpUsageCountersJob.php` +5 |
| `replay.ts:223-228` hardcodes `conflict_code = 'form_updated'` on a client-side version guard. | `M14` | 1200 | *hub files only* |
| The command gate reads `README.md` only, and three other documents carry runnable command blocks. | `M59` | 4801 | `docs/TESTING-GUIDE.md` · `docs/ACCESS-MATRIX.md` · `docs/deployment-infrastructure.md` |
| All sixteen synchronous dispatch listeners could now be `ShouldQueue`, and nothing has decided whether they should be. | `M3` | 695 | — *no file harvested* |
| Nothing checks that a `§D<n>` citation names a section whose text supports it. | `M7` | 5126 | — *no file harvested* |
| The sync surface's read and write are gated on different permission families, so no single non-admin role can complete the offline loop. | `M13` | 1901 | — *no file harvested* |
| the dashboard card ranks the whole tenant to compute three numbers that need no ranking. | `M26` | 2083 | — *no file harvested* |
| `loop assess` can only see what a row says about ITSELF, and two blind spots are now measured. | `M55` | 5075 | — *no file harvested* |
| A pre-existing mixed-case `public_slug` row would have been taken dark by M61, and nothing in the repository can tell whether one exists. | `M61` | 4854 | — *no file harvested* |

## Hub files — a batch may touch at most one

Derived, not listed: a file cited by **3 or more** open rows. These are
mostly meta-files rather than product code, which is why the rows that share them are not
really coupled — and why excluding them is what makes the queue above separable.

| File | Open rows citing it |
|---|---|
| `docs/data-dictionary.md` | 10 |
| `routes/api.php` | 9 |
| `routes/tenant.php` | 9 |
| `.github/workflows/ci.yml` | 8 |
| `scripts/state.php` | 7 |
| `scripts/backlog-triage.php` | 6 |
| `docs/security-threat-model.md` | 6 |
| `PROGRESS_ARCHIVE.md` | 6 |
| `docs/api-specification.md` | 6 |
| `CLAUDE.md` | 5 |
| `openapi.json` | 5 |
| `docs/architecture/technical-architecture.md` | 4 |
| `app/Providers/AppServiceProvider.php` | 4 |
| `scripts/mutate.php` | 3 |
| `tests/e2e/public-runtime-offline.spec.ts` | 3 |
| `resources/public-runtime/App.vue` | 3 |
| `scripts/tracker-lint-controls.php` | 3 |
| `docs/backlog-triage.md` | 3 |
| `scripts/loop.php` | 3 |
| `scripts/pre-push-guard.php` | 3 |
| `composer.json` | 3 |
| `resources/public-runtime/lib/replay.ts` | 3 |
| `docs/webhook-integration-design.md` | 3 |
| `docs/gate-baselines.md` | 3 |
| `app/Models/Form.php` | 3 |
| `database/migrations/2026_07_06_000201_create_forms_table.php` | 3 |
| `docs/PRD.md` | 3 |
| `routes/console.php` | 3 |

## Suggested next batch

`D13` fixes the rule: **3–4 rows, no two citing the same non-hub file, at most one row touching a hub file.**
This is the greedy pick off the top of the queue above — a proposal to check, not a schedule.
A row whose files were not harvested cannot be checked for collision and is not proposed.

- **docs/feature-backlog.md:4281** — `M42` · *no non-hub file*
- **docs/feature-backlog.md:4664** — `M58` · `tests/Feature/Migrations/DocumentedDefaultDriftTest.php`
- **docs/feature-backlog.md:5797** — `M70` · `docs/audit-compliance-logging-spec.md`
- **docs/feature-backlog.md:5854** — `M71` · `scripts/tracker-surgery.php`

## What this file cannot see

**31 open row(s) cite no file this script could resolve**, and **14 carry at least one citation that does not resolve.** They are ranked below rows whose citations
all resolve, because a row must be re-derived before it is worked rather than merely worked —
but the ranking is the only consequence: an unresolvable citation is not evidence the row is
wrong. **Batch these by hand**, since a collision check needs files to compare.

Rows with no harvested file:

- `docs/feature-backlog.md:795` — A tabular rule that maps no Submission ID column still duplicates on an unconfirmed retry, and the durable fix is in the rule EDITOR rather than the adapter.
- `docs/feature-backlog.md:830` — `SlackConnector::deliver()` has the same non-idempotent shape and is deliberately not covered.
- `docs/feature-backlog.md:1912` — `promote()` re-asserts the version is published BEFORE the lock and never again under it.
- `docs/feature-backlog.md:2376` — The tenant-facing SSO domains card on `/settings/sso` does not exist, so verification is operator-assisted.
- `docs/feature-backlog.md:2391` — `MemberController::invite()` validates `['required', 'email', 'max:255']` and a role, with no domain-ownership check.
- `docs/feature-backlog.md:2402` — Self-registration remains a way to occupy an address in a domain you do not control.
- `docs/feature-backlog.md:1381` — The storage-quota line counts strangers' submissions.
- `docs/feature-backlog.md:4058` — Every hand-off prescribes a Pint command that scans ~40 fewer files than CI does.
- `docs/feature-backlog.md:3262` — `PUT /user/profile-information` is a second mail cannon, and it is the one Fortify write route left deliberately unbound.
- `docs/feature-backlog.md:1941` — The promote route still documents neither of its two 403 causes nor its 422, and the generator cannot narrow a refusal family to one route.
- `docs/feature-backlog.md:1809` — The per-item `error.code` is the integrator's real branching key on `/sync/submissions`, and the contract still publishes it as an unconstrained string.
- `docs/feature-backlog.md:6182` — `DROP_BYTE_LIMIT` is an absolute constant unindexed to a ceiling `R1`'s own discipline keeps ratcheting, so sampling bias is the symptom and the missing index …
- `docs/feature-backlog.md:6447` — What a media cell should SAY beyond a filename, and what shape a grid answer takes in a spreadsheet, are two product questions `M74` deliberately did not answe…
- `docs/feature-backlog.md:6664` — The brand sweep now renews every entry it touches, which resets `maxEntries` eviction from recency-of-use to sweep order — and nobody has decided whether the s…
- `docs/feature-backlog.md:7033` — The data dictionary documents `tenants.status` defaulting to a value that is not a legal case of the enum it names.
- `docs/feature-backlog.md:7042` — `forms.timezone` is documented as defaulting to NULL and the database defaults it to `'UTC'`.
- `docs/feature-backlog.md:7049` — Two columns are documented under the wrong table in the data dictionary.
- `docs/feature-backlog.md:7056` — Nine live tables have no dictionary section, and they are not all framework scaffolding.
- `docs/feature-backlog.md:7079` — `MdsSegmentedControl`'s stretch-clamp census is short by two consumers, one candidate fix is a no-op, and the 30px figure cannot be reproduced.
- `docs/feature-backlog.md:810` — M5's reconciliation asks "is this SUBMISSION in the destination", not "is THIS DELIVERY's row in the destination", so two rules writing one submission to one t…
- `docs/feature-backlog.md:823` — A 5xx that arrives AFTER the provider committed is still re-driven.
- `docs/feature-backlog.md:875` — A rotated token can still be lost in the one-UPDATE window M6 left.
- `docs/feature-backlog.md:2557` — `users.last_active_tenant_id` has no writer anywhere in `app/`.
- `docs/feature-backlog.md:4843` — The database's uniqueness domain and the runtime's resolution domain now disagree about case, and `FormSlug::isTaken()` is the third resolver.
- `docs/feature-backlog.md:6343` — `SubmissionFinalizer` is a SECOND writer of `submission_answers.form_version_id`, and it would erase the evidence of a divergence rather than record it.
- `docs/feature-backlog.md:695` — All sixteen synchronous dispatch listeners could now be `ShouldQueue`, and nothing has decided whether they should be.
- `docs/feature-backlog.md:5126` — Nothing checks that a `§D<n>` citation names a section whose text supports it.
- `docs/feature-backlog.md:1901` — The sync surface's read and write are gated on different permission families, so no single non-admin role can complete the offline loop.
- `docs/feature-backlog.md:2083` — the dashboard card ranks the whole tenant to compute three numbers that need no ranking.
- `docs/feature-backlog.md:5075` — `loop assess` can only see what a row says about ITSELF, and two blind spots are now measured.
- `docs/feature-backlog.md:4854` — A pre-existing mixed-case `public_slug` row would have been taken dark by M61, and nothing in the repository can tell whether one exists.
